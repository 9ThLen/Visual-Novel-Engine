{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the studio window is allowed to do. Core only: no filesystem, no shell, no dialog, no HTTP plugin. The author's projects live in the webview's own storage, which needs no permission. The AI bridge is started by three commands this application defines itself, which take no arguments and resolve the executable from the bundle's own resource directory - app commands invoked from a local origin are not gated by capabilities, and no shell permission is involved.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
