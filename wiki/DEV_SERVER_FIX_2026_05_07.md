# DEV_SERVER_FIX_2026_05_07

## Підсумок: Metro сервер ПРАЦЮЄ

### Що з'ясували:
1. **Metro сервер працює!** Підтверджено через `netstat` — процес слухає порт 8081 (перевірено pid 2315)
2. Проблема була не в конфігурації, а в тому як ми перевіряли статус
3. `app.config.js` виправлено: `module.exports` → `export default` (через `"type": "module"` в package.json)
4. TypeScript понижено з 5.9.3 до 5.7.2 (сумісно з RN 0.81.5)

### Рішення:
- Metro сервер запускається командою: `cd /mnt/d/Programs/D/visual_novel_engine && npx expo start --clear`
- Процес працює, але вивід може не відображатися в деяких режимах запуску
- Перевірка: `netstat -tlnp | grep 8081` має показати процес node на порту 8081

### Що не допомогло:
- Видалення `metro.config.js` — Metro працює і без нього (використовує дефолтний)
- Створення симлінків — проблема не в довжині шляхів WSL
- Перестановка `node_modules` — залежності були ок

### Гіпотеза:
Сервер працює, але при запуску через background/pty modes вивід не завжди захоплюється коректно. В звичайному режимі (інтерактивному) все має працювати.

### Наступні кроки:
1. Використовувати Metro для розробки (порт 8081)
2. Додати mocks для `expo-audio` у тести
3. Рефакторинг `BlockFlowCanvas.tsx`

---
*Оновлено: 2026-05-07 16:15*

## Пов'язані сторінки
- [[audit-report-2026-05-07|Звіт про аудит 2026-05-07]]
