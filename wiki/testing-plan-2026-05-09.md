# План тестування та подальших дій — 2026-05-09

**Дата:** 2026-05-09  
**Статус:** Очікує тестування користувачем  
**Пріоритет:** Високий

## Що було зроблено

Виправлено 7 критичних помилок, які призводили до білого екрану:
1. 6 подвійних ком у `AsyncStorage.setItem()` (story-context.tsx)
2. Подвійні номери рядків у story-context.tsx
3. Відсутній `useState` для `isInitialized` (tabs/index.tsx)
4. Зламаний симлінк expo-asset (pnpm на WSL)
5. Відсутній babel-плагін `react-native-reanimated/plugin` (користувач)
6. Файл `lego-editor.tsx` переміщено в `app/tabs/` (користувач)
7. Видалено `unstable_settings` з `_layout.tsx` (користувач)

## Що зробити зараз (ТЕСТУВАННЯ)

### Крок 1: Запустити Metro
```bash
cd /mnt/d/Programs/D/visual_novel_engine
npx expo start --clear
```
Для телефону з WSL:
```bash
npx expo start --tunnel
```

### Крок 2: Перевірити головний екран
- [ ] Відображається список історій (Stories)
- [ ] Кнопки "Edit" та налаштувань працюють
- [ ] Demo-історії завантажуються

### Крок 3: Перевірити вкладку LEGO
- [ ] Вкладка "LEGO" відображається в нижньому меню
- [ ] Canvas: атоми відображаються, перетягуються
- [ ] Canvas: магнітне притягування (snap) працює
- [ ] Timeline: події додаються, переставляються
- [ ] Graph: вузли та зв'язки візуалізуються
- [ ] Додавання нової сцени

### Крок 4: Перевірити читання
- [ ] Відкриття demo-історії
- [ ] Текст відображається з ефектом друкарської машинки
- [ ] Вибори працюють (переходи між сценами)

### Крок 5: Знайти runtime-помилки
- [ ] Перевірити консоль Metro на червоні помилки
- [ ] Зафіксувати всі помилки текстом

## Відомі проблеми що можуть виникнути

| Проблема | Рішення |
|----------|---------|
| `UnableToResolveError` для useReaderAudio | Очистити кеш: видалити `.expo/` та перезапустити Metro з `--clear` |
| Білий екран після зміни коду | Перезавантажити в Expo Go (потрясти телефон → Reload) |
| Expo Go не підключається (WSL) | Використовувати `--tunnel` замість LAN |
| Анімації не працюють | Перевірити що `react-native-reanimated/plugin` у babel.config.cjs |

## Наступні кроки (після тестування)

### Якщо додаток працює:
1. Допрацювати drag-and-drop молекул між сценами
2. Підключити scene-store.ts до AsyncStorage (автозбереження)
3. Додати експорт/імпорт JSON
4. Покращити UI/UX (zoom/pan, анімації переходів)
5. Додати тести для LegoCanvas, TimelineEditor

### Якщо є помилки:
1. Надіслати текст помилки (скріншот або копі-паст)
2. Вказати на якому екрані/дії сталася помилка
3. Я виправлю і перезапущу Metro

## Пов'язані сторінки
- [[runtime-fixes-2026-05-09|Виправлення runtime-помилок 2026-05-09]]
- [[next-session-plan-2026-05-10|План на сесію 2026-05-10]]
- [[log|Журнал подій]]
- [[index|Головна сторінка Wiki]]
