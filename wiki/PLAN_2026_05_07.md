# 2026-05-07 - Execution Plan

## Мета: Запустити Metro dev server + підготовка до розробки

### Контекст
Metro bundler не запускався з 2026-05-06. Навіть мінімальні конфіги (.js файли) не працювали. Порт 8081 вільний, але сервер не стартував і не видавав логів.

### Гіпотези для перевірки (у порядку пріоритетності)

1. **Expo 54 + React Native 0.81.5 + TypeScript 5.9.3** — можливо конфлікт версій
2. **WSL Windows path length issues** — `node_modules/.pnpm` створює дуже довгі шляхи
3. **node_modules пошкоджені або конфліктують**
4. **Конфігураційні файли** — app.config.js та metro.config.js

### Кроки виконання

1. [x] Перевірити Node.js версію (`node --version`) — v22.22.2 ✅
2. [x] Перевірити чи `pnpm` коректно встановив залежності ✅
3. [x] Видалити `node_modules` та `pnpm-lock.yaml` ✅
4. [x] Перевстановити: `pnpm install` ✅ (1115 пакетів)
5. [x] Спробувати запустити `npx expo start --clear` ✅
   - **Виявилося:** Metro ПРАЦЮЄ! (підтверджено через `netstat` — pid 2315 слухав 8081 порт)
   - Проблема була в тому як ми перевіряли (вивід не видно через особливості запуску в фоні)
6. [x] TypeScript понижено до 5.7.2 ✅ (сумісний з RN 0.81.5)
7. [x] Додати mocks для `expo-audio` у тести ✅
   - `audio-manager.test.ts` — 6/6 тестів проходять
   - Експортовано клас `AudioManager` з `lib/audio-manager.ts`
   - Виправлено імпорт у тесті (статичний замість динамічного)
8. [x] Рефакторинг `CharacterLibraryManager.tsx` (720 рядків) ✅
   - Виділено компонент `CharacterList` в окремий файл
   - `CharacterLibraryManager.tsx` тепер використовує `<CharacterList ... />`

### Що не допомогло:
- Видалення `metro.config.js` — Metro працює і без нього (використовує дефолтний)
- Створення симлінків — проблема не в довжині шляхів WSL
- Перестановка `node_modules` — залежності були ок

### Успіх = Metro сервер працює! ✅
- Підтверджено: `netstat -tlnp | grep 8081` показує процес node на порту 8081
- `app.config.js` виправлено: `module.exports` → `export default` (через `"type": "module"` в package.json)

### Додаткові завдання (виконано):
1. ✅ Тести проходять (6/6 для audio-manager.test.ts)
2. ✅ Мінімальний рефакторинг CharacterLibraryManager.tsx
3. ⏳ Продовжити покриття тестами (наступна сесія)

---

## Підсумок сесії:
- Metro dev server ПРАЦЮЄ ✅
- TypeScript 5.7.2, Expo 54.0.34, React Native 0.81.5
- Тести Vitest налаштовані (typecheck:false)
- Код оптимізовано (useCallback, useMemo)
- Wiki оновлено

## Пов'язані сторінки
- [[audit-report-2026-05-07|Звіт про аудит 2026-05-07]]

*Створено: 2026-05-07 16:30*
