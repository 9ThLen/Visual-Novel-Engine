---
phase: 05-legacy-cleanup-and-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stores/use-app-store.ts
  - lib/canonical-scene.ts
  - lib/runtime-story.ts
  - lib/story-hooks.ts
  - app/editor.tsx
  - lib/scene-record-adapter.ts
  - __tests__/unit/lib/runtime-story.test.ts
  - __tests__/unit/stores/use-app-store-canonical.test.ts
  - __tests__/unit/stores/use-app-store-scene-operations.test.ts
autonomous: true
requirements:
  - ARCH-03
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - "Legacy Story/StoryScene no longer acts as the default production path for scene creation, runtime reconstruction or store selectors"
    - "Compatibility adapters remain only where explicitly required for migration or import/export boundaries"
    - "New stories and active production flows are created and reconstructed from canonical scene records first"
  artifacts:
    - path: stores/use-app-store.ts
      provides: Canonical-first app state and reduced steady-state legacy dependence
      contains: "sceneRecordsByStory"
    - path: lib/runtime-story.ts
      provides: Canonical-first runtime contract with explicit compatibility boundaries
      contains: "canonical"
    - path: app/editor.tsx
      provides: New story creation aligned to canonical scene records instead of legacy-only Story shape
      contains: "sceneRecordsByStory"
  key_links:
    - from: app/editor.tsx
      to: stores/use-app-store.ts
      via: canonical story creation path
      pattern: "saveSceneRecord"
    - from: lib/story-hooks.ts
      to: lib/runtime-story.ts
      via: compatibility isolation for runtime reconstruction
      pattern: "buildRuntimeStorySnapshot"
---

<objective>
Isolate or remove remaining legacy production dependencies so the steady-state app path is canonical-first by default.

Purpose: Finish the architectural transition started in Phases 1-4 by ensuring legacy adapters support only explicit compatibility scenarios instead of normal editor/runtime behavior.
Output: Canonical-first steady-state paths, reduced legacy fallbacks, and focused regression coverage around the new boundary.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-legacy-cleanup-and-quality-gate/05-CONTEXT.md
@stores/use-app-store.ts
@lib/canonical-scene.ts
@lib/runtime-story.ts
@lib/story-hooks.ts
@app/editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for explicit legacy boundary behavior</name>
  <files>__tests__/unit/lib/runtime-story.test.ts, __tests__/unit/stores/use-app-store-canonical.test.ts, __tests__/unit/stores/use-app-store-scene-operations.test.ts</files>
  <read_first>lib/runtime-story.ts, lib/canonical-scene.ts, stores/use-app-store.ts, app/editor.tsx, existing unit tests</read_first>
  <action>Add focused tests that define the final Phase 5 boundary: canonical scene records are the default source for active production flows, new story creation does not require legacy `StoryScene`, and any remaining compatibility fallback is explicit and narrow. Verify the tests fail first for the expected reason before implementation.</action>
  <verify>Run only the new/updated legacy-boundary tests and confirm failure is caused by missing canonical-first cleanup behavior.</verify>
  <acceptance_criteria>
    - Tests describe canonical-first steady-state behavior
    - Tests fail before implementation for the expected reason
    - At least one test proves legacy fallback is no longer the default production branch
  </acceptance_criteria>
  <done>The desired compatibility boundary is captured in executable tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor steady-state store/runtime paths to be canonical-first</name>
  <files>stores/use-app-store.ts, lib/canonical-scene.ts, lib/runtime-story.ts, lib/story-hooks.ts, lib/scene-record-adapter.ts</files>
  <read_first>stores/use-app-store.ts, lib/canonical-scene.ts, lib/runtime-story.ts, lib/story-hooks.ts, lib/types.ts, .planning/phases/05-legacy-cleanup-and-quality-gate/05-CONTEXT.md</read_first>
  <action>Reduce or isolate legacy behavior in the steady-state path. Remove silent fallback from production selectors/runtime reconstruction where Phases 1-4 already provide canonical data, and keep compatibility adapters only behind explicit import/export or migration boundaries. If some fallback must remain for backward compatibility, rename or structure it so the production path is unmistakably canonical-first.</action>
  <verify>Run the updated legacy-boundary tests plus existing canonical/runtime/story-flow suites.</verify>
  <acceptance_criteria>
    - Active editor/runtime/store flows no longer depend on legacy `StoryScene` as a normal branch
    - Compatibility fallback, if retained, is explicit and narrow
    - Regression tests for Phases 1-4 still pass
  </acceptance_criteria>
  <done>The app's steady-state scene data flow is clearly canonical-first and legacy dependence is reduced to isolated compatibility edges.</done>
</task>

<task type="auto">
  <name>Task 3: Align new story creation and reachable entry points to the canonical path</name>
  <files>app/editor.tsx, stores/use-app-store.ts, lib/story-hooks.ts</files>
  <read_first>app/editor.tsx, stores/use-app-store.ts, lib/story-hooks.ts, hooks/useReaderInitialization.ts</read_first>
  <action>Update entry-point flows so newly created stories and scenes are born in canonical form rather than first becoming legacy `Story` objects. Make any remaining compatibility-only entry points explicit, and remove or isolate dormant production-adjacent legacy hooks if the reachability check confirms they are unused.</action>
  <verify>Run focused route/store tests and sanity-check that create -> open -> edit -> play still follows the canonical path.</verify>
  <acceptance_criteria>
    - `app/editor.tsx` no longer seeds new stories through a legacy-only primary shape
    - Reachable scene lifecycle entry points are aligned to canonical store/runtime helpers
    - Unused legacy entry points are either removed or clearly marked isolated
  </acceptance_criteria>
  <done>New user data enters the system through the canonical path instead of through compatibility shapes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Updated legacy-boundary tests pass
- [ ] Existing Phase 1-4 focused suites still pass
- [ ] No active production entry point still relies on legacy `StoryScene` as its default steady-state branch
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Legacy production dependence is reduced to explicit compatibility edges
- The steady-state app path is canonical-first end to end
</success_criteria>

<output>
After completion, create `.planning/phases/05-legacy-cleanup-and-quality-gate/05-01-SUMMARY.md`
</output>
