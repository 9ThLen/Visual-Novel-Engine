---
phase: 05-legacy-cleanup-and-quality-gate
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
  - wiki/architecture-reference.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-legacy-cleanup-and-quality-gate/05-VERIFICATION.md
  - .planning/phases/05-legacy-cleanup-and-quality-gate/05-MANUAL-QA.md
  - __tests__/**
  - app/editor.tsx
  - components/editor/SceneManager.tsx
  - components/editor/StoryFlowScreen.tsx
autonomous: true
requirements:
  - ARCH-04
  - QUAL-01
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - "README, architecture docs and planning docs describe the same canonical scene/runtime/story-flow architecture that the code now implements"
    - "Critical path has targeted verification artifacts for create/open/edit/save/reopen/preview/play/save-load/story-flow flows"
    - "Final quality gate is explicit about remaining blockers and fixes the typed-route baseline if it is the last barrier to clean verification"
  artifacts:
    - path: .planning/phases/05-legacy-cleanup-and-quality-gate/05-VERIFICATION.md
      provides: Final verification matrix for Phase 5 and prior critical paths
      min_lines: 25
    - path: .planning/phases/05-legacy-cleanup-and-quality-gate/05-MANUAL-QA.md
      provides: Manual smoke scenarios covering stabilized user flows
      min_lines: 25
    - path: README.md
      provides: Updated canonical-first project overview and usage flow
      contains: "SceneRecord"
  key_links:
    - from: README.md
      to: docs/SCENE-MODEL-CONTRACT.md
      via: canonical architecture documentation
      pattern: "SceneRecord"
    - from: .planning/REQUIREMENTS.md
      to: .planning/STATE.md
      via: completed vs pending requirement traceability
      pattern: "QUAL-01"
---

<objective>
Close the final quality gate with aligned docs, critical-path verification artifacts and the cleanest possible project baseline.

Purpose: Turn the stabilized refactor into a trustworthy handoff state by removing documentation drift, capturing manual QA and resolving final verification blockers where feasible.
Output: Updated docs, verification packs, targeted critical-path coverage and an explicit final gate result.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/05-legacy-cleanup-and-quality-gate/05-CONTEXT.md
@README.md
@wiki/architecture-reference.md
@docs/SCENE-MODEL-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests or verification specs for remaining critical-path gaps</name>
  <files>__tests__/**, .planning/phases/05-legacy-cleanup-and-quality-gate/05-VERIFICATION.md</files>
  <read_first>Phase 1-4 summaries, current unit tests, SceneManager/StoryFlow/Preview/useAutoSave entry points</read_first>
  <action>Identify the highest-value remaining gaps on the create/open/edit/save/reopen/preview/play/save-load/story-flow path and add targeted tests or explicit verification specs before implementation. Keep this narrow and signal-heavy: prefer tests that cover route/store/component seams that previous phases intentionally left as pure-helper-only coverage.</action>
  <verify>Run the newly added tests/specs first and confirm any RED state is caused by the missing quality-gate behavior you intend to add.</verify>
  <acceptance_criteria>
    - Remaining critical-path gaps are explicitly listed
    - New tests or verification specs exist for the highest-risk uncovered seams
    - Red/green flow is followed for any new automated coverage
  </acceptance_criteria>
  <done>The final quality-gate scope is executable and not just descriptive.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs and planning artifacts to match the actual architecture</name>
  <files>README.md, wiki/architecture-reference.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>README.md, wiki/architecture-reference.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, docs/SCENE-MODEL-CONTRACT.md, current code paths</read_first>
  <action>Refresh the user-facing and planning-facing docs so they describe the actual canonical scene architecture, current editor/runtime/story-flow paths, and the real phase completion state. Mark historical docs or plans as historical when they would otherwise mislead future work. Keep the updates factual and tied to current code, not aspirational design.</action>
  <verify>Diff the updated docs against current code paths and ensure no section still describes React Context / Node Editor / legacy-only runtime as the primary architecture.</verify>
  <acceptance_criteria>
    - README matches the current canonical-first app flow
    - architecture docs describe current state management and scene data flow accurately
    - planning docs reflect completed Phases 1-4 and pending/complete requirement statuses truthfully
  </acceptance_criteria>
  <done>Documentation becomes a reliable source of truth instead of a brownfield hazard.</done>
</task>

<task type="auto">
  <name>Task 3: Produce the final verification pack and clean the last feasible blockers</name>
  <files>.planning/phases/05-legacy-cleanup-and-quality-gate/05-VERIFICATION.md, .planning/phases/05-legacy-cleanup-and-quality-gate/05-MANUAL-QA.md, app/editor.tsx, components/editor/SceneManager.tsx, components/editor/StoryFlowScreen.tsx</files>
  <read_first>.planning/ROADMAP.md, prior phase summaries, current verification commands, typed-route errors from `pnpm check`</read_first>
  <action>Create a final verification pack with manual smoke scenarios for each stabilized flow across Phases 1-4 and Phase 5. Then run the final automated verification commands. If Expo Router typed-route issues remain the only blocker to a clean `pnpm check`, fix them in this wave so the milestone ends with the cleanest possible baseline. If any blocker cannot be resolved safely, document it explicitly in the verification pack with scope and impact.</action>
  <verify>Run the agreed focused test suite and `pnpm check`; record the result in `05-VERIFICATION.md` and `05-MANUAL-QA.md`.</verify>
  <acceptance_criteria>
    - Manual QA scenarios exist for all key stabilized flows
    - Final automated verification is recorded with pass/blocker status
    - Typed-route baseline is fixed if it is the last safe barrier to clean typecheck
  </acceptance_criteria>
  <done>The milestone ends with a clear final gate result and reproducible verification steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Final critical-path tests pass
- [ ] `README`, architecture docs and planning docs match current code
- [ ] `05-VERIFICATION.md` and `05-MANUAL-QA.md` exist and are usable by another developer
- [ ] `pnpm check` is either clean or any remaining blocker is explicitly documented as out-of-scope with rationale
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Docs and planning artifacts reflect the real canonical architecture
- The project ends Phase 5 with an explicit, trustworthy quality gate result
</success_criteria>

<output>
After completion, create `.planning/phases/05-legacy-cleanup-and-quality-gate/05-02-SUMMARY.md`
</output>
