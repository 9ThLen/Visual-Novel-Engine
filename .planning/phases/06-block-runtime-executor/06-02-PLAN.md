---
phase: 06-block-runtime-executor
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - components/story-reader-responsive.tsx
  - app/reader.tsx
  - hooks/useReaderInitialization.ts
  - lib/runtime-story.ts
  - lib/canonical-scene.ts
autonomous: true
requirements:
  - BLOCK-02
  - BLOCK-03
user_setup: []
must_haves:
  truths:
    - "Reader executes blocks from TimelineStep[] via useSceneExecutor, not via lossy StoryScene adapter"
    - "useReaderInitialization returns TimelineStep[] + SceneRecord instead of StoryScene"
    - "sceneRecordToStoryScene is removed from production runtime path"
    - "Block variables and previous block results are accessible to subsequent blocks through SceneState"
  artifacts:
    - path: components/story-reader-responsive.tsx
      provides: Reader component updated to consume TimelineStep[] + executor state
      contains: "useSceneExecutor"
    - path: app/reader.tsx
      provides: Reader entry point aligned to canonical timeline execution
      contains: "useSceneExecutor"
    - path: hooks/useReaderInitialization.ts
      provides: Returns canonical data (SceneRecord, TimelineStep[]) instead of legacy StoryScene
      contains: "TimelineStep"
---

<objective>
Integrate `useSceneExecutor` into the reader pipeline. Replace the `StoryScene`-based reader flow with a block-by-block executor flow so all 12 block types render correctly.

Purpose: The reader currently sees only ~4 block types via `sceneRecordToStoryScene`. After this plan, all 12 types execute and produce visible output.
Output: Updated `StoryReaderResponsive` (or new `BlockReader` component), updated `app/reader.tsx`, updated `useReaderInitialization`.
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-block-runtime-executor/06-CONTEXT.md
@.planning/phases/06-block-runtime-executor/06-01-PLAN.md
@components/story-reader-responsive.tsx
@app/reader.tsx
@hooks/useReaderInitialization.ts
@lib/runtime-story.ts
@lib/engine/types.ts
@lib/engine/useSceneExecutor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sceneRecordToStoryScene from production runtime path</name>
  <files>lib/runtime-story.ts, lib/canonical-scene.ts, lib/scene-record-adapter.ts</files>
  <read_first>lib/runtime-story.ts, lib/canonical-scene.ts, lib/scene-record-adapter.ts</read_first>
  <action>
    In `lib/runtime-story.ts`:
    - Replace any calls to `sceneRecordToStoryScene` with direct TimelineStep[] access.
    - If `resolvePreviewTimeline` or similar functions convert to StoryScene, update them to return `TimelineStep[]` instead.

    In `lib/canonical-scene.ts`:
    - Replace `sceneRecordsToStoryScenes` (or similar) to return scene records directly.

    In `lib/scene-record-adapter.ts`:
    - Keep `sceneRecordToStoryScene` but mark it as `@deprecated — use useSceneExecutor instead`.
    - Keep `storySceneToSceneRecordDraft` for legacy import scenarios only.

    Ensure all production code paths now pass `TimelineStep[]` instead of `StoryScene`.
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - `runtime-story.ts` no longer calls `sceneRecordToStoryScene` in its production path
    - `canonical-scene.ts` no longer depends on `sceneRecordToStoryScene` for production selectors
    - `sceneRecordToStoryScene` is marked deprecated
    - `npm run check` passes
  </acceptance_criteria>
  <done>Production runtime path no longer depends on lossy StoryScene adapter</done>
</task>

<task type="auto">
  <name>Task 2: Update useReaderInitialization to return canonical data</name>
  <files>hooks/useReaderInitialization.ts</files>
  <read_first>hooks/useReaderInitialization.ts</read_first>
  <action>
    Update `useReaderInitialization` to return `TimelineStep[]` and `SceneRecord` instead of (or in addition to) `StoryScene`.

    The hook should:
    - Still load story data from the store
    - Return `sceneRecord: SceneRecord | null` instead of `currentScene: StoryScene`
    - Return `timeline: TimelineStep[]` extracted from the scene record
    - Keep backward compatibility if other consumers rely on `currentScene`
    - If backward compat is needed, compute `currentScene` via deprecated adapter and add a `@deprecated` JSDoc tag

    Update all consumers in `app/reader.tsx` to use the new return values.
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - `useReaderInitialization` returns `sceneRecord` and `timeline`
    - Existing consumers updated
    - `npm run check` passes
  </acceptance_criteria>
  <done>Reader initialization provides canonical timeline data</done>
</task>

<task type="auto">
  <name>Task 3: Update StoryReaderResponsive to use executor</name>
  <files>components/story-reader-responsive.tsx</files>
  <read_first>components/story-reader-responsive.tsx, lib/engine/useSceneExecutor.ts</read_first>
  <action>
    Update `StoryReaderResponsive` to accept `TimelineStep[]` instead of `StoryScene`.

    New props interface:
    ```typescript
    interface Props {
      timeline: TimelineStep[];
      initialVariables?: Record<string, string | number | boolean>;
      onTransition: (targetSceneId: string | null) => void;
      onChoiceSelect?: (optionId: string) => void;
      isLoading?: boolean;
      settings?: Partial<UserSettings>;
      onHistoryVisibleChange?: (visible: boolean) => void;
    }
    ```

    Inside the component:
    - Call `useSceneExecutor(timeline, { initialVariables })`
    - Render based on `sceneState`:
      - Background: `sceneState.backgroundAssetId`
      - Characters: `sceneState.characters[]` via existing `CharacterDisplay`
      - Text/Dialogue: current yielding block via `currentStepIndex`
      - Choices: `sceneState.currentChoices`
      - Effects: `sceneState.activeEffects`
      - Music: `sceneState.musicTrackId` / `sceneState.musicPlaying`
    - `onContinue` → calls `advance()` or `onTransition()` if `isTransitioning`
    - `onChoiceSelect` → calls `selectChoice(optionId)`
    - Typewriter and dialogue rendering stay as-is, just driven by executor state instead of `StoryScene` fields

    If the refactoring is too invasive, create a thin wrapper component `BlockReader` that uses `useSceneExecutor` and renders the existing `StoryReaderResponsive` internals. Prefer direct modification of `StoryReaderResponsive`.
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - StoryReaderResponsive uses useSceneExecutor
    - All 12 block types influence the reader display
    - Typewriter, dialogue history, choice selection still work
    - Music/audio triggers work via executor state
    - `npm run check` passes
  </acceptance_criteria>
  <done>Reader renders all block types through the executor</done>
</task>

<task type="auto">
  <name>Task 4: Update app/reader.tsx navigation to use SceneRecord transitions</name>
  <files>app/reader.tsx</files>
  <read_first>app/reader.tsx, components/story-reader-responsive.tsx</read_first>
  <action>
    Update `app/reader.tsx` to work with the new reader props:
    - Pass `timeline` from `useReaderInitialization` to `StoryReaderResponsive`
    - Handle `onTransition` to navigate to the next scene using `playbackState`
    - Remove or update any code that accesses `story.scenes[sceneId]` (legacy `Story` shape) for scene data
    - Scene navigation should use the canonical scene resolution path: resolve scene by ID from `sceneRecordsByStory`
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - reader.tsx passes TimelineStep[] to StoryReaderResponsive
    - Scene transitions work via onTransition callback
    - No remaining dependency on legacy story.scenes for scene data
    - `npm run check` passes
  </acceptance_criteria>
  <done>Reader entry point is aligned to canonical execution</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check` passes
- [ ] Reader displays blocks from all 12 types
- [ ] sceneRecordToStoryScene deprecated in production path
- [ ] Scene navigation still works
- [ ] Choices, dialogue history, typewriter all functional
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Reader fully executes all block types through useSceneExecutor
- Lossy adapter removed from production path
- `npm run check` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-block-runtime-executor/06-02-SUMMARY.md`
</output>
