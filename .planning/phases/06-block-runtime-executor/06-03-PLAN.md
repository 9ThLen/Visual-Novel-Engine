---
phase: 06-block-runtime-executor
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - components/editor/PreviewScreen.tsx
  - lib/runtime-story.ts
autonomous: true
requirements:
  - BLOCK-04
user_setup: []
must_haves:
  truths:
    - "PreviewScreen uses the same useSceneExecutor hook as the reader — no duplicated execution logic"
    - "Preview shows all 12 block types, matching what the reader will display"
  artifacts:
    - path: components/editor/PreviewScreen.tsx
      provides: Editor preview aligned to canonical block execution
      contains: "useSceneExecutor"
---

<objective>
Update the editor's PreviewScreen to use `useSceneExecutor` instead of `resolvePreviewTimeline`/`sceneRecordToStoryScene`.

Purpose: The preview should show exactly what the reader will display — all 12 block types, not just the 4 that survived the legacy adapter.
Output: Updated PreviewScreen that shares the same executor hook as the reader.
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-block-runtime-executor/06-CONTEXT.md
@.planning/phases/06-block-runtime-executor/06-01-PLAN.md
@components/editor/PreviewScreen.tsx
@lib/runtime-story.ts
@lib/engine/useSceneExecutor.ts
@lib/engine/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PreviewScreen to use useSceneExecutor</name>
  <files>components/editor/PreviewScreen.tsx</files>
  <read_first>components/editor/PreviewScreen.tsx, lib/engine/useSceneExecutor.ts</read_first>
  <action>
    Update `PreviewScreen` to:
    - Import and use `useSceneExecutor` instead of `resolvePreviewTimeline`
    - Get `TimelineStep[]` from the currently edited scene record
    - Pass the timeline to `useSceneExecutor`
    - Render a simplified reader view based on executor state:
      - Show background, characters, text/dialogue, choices
      - Show current step info (step index / total)
      - Provide "advance" button for yielding blocks
      - Show choice buttons when `currentChoices` is set
    - Match the reader's behavior so the preview is an accurate representation
    - Keep the existing preview overlay / modal pattern
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - PreviewScreen uses useSceneExecutor
    - Preview shows all block types
    - No dependency on resolvePreviewTimeline or sceneRecordToStoryScene
    - `npm run check` passes
  </acceptance_criteria>
  <done>Preview uses the same executor as the reader</done>
</task>

<task type="auto">
  <name>Task 2: Clean up unused runtime-story preview functions</name>
  <files>lib/runtime-story.ts</files>
  <read_first>lib/runtime-story.ts</read_first>
  <action>
    In `lib/runtime-story.ts`, remove or deprecate any functions that are no longer needed:
    - `resolvePreviewTimeline` if no longer called by PreviewScreen
    - Any other adapter functions that were only used by the preview path
    - Keep functions used by story-hooks.ts or other consumers
    - Mark removed functions with a brief comment explaining their replacement
  </action>
  <verify>Run `npm run check`</verify>
  <acceptance_criteria>
    - Unused preview adapter functions removed or deprecated
    - All imports updated
    - `npm run check` passes
  </acceptance_criteria>
  <done>Codebase cleaned of unused sceneRecordToStoryScene calls</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check` passes
- [ ] PreviewScreen uses useSceneExecutor
- [ ] Preview shows all 12 block types correctly
- [ ] Unused adapter functions cleaned up
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Preview and reader share the same executor
- Codebase is clean of unused legacy adapter calls in production paths
- `npm run check` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-block-runtime-executor/06-03-SUMMARY.md`
</output>
