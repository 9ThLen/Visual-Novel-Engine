---
phase: 03-runtime-and-persistence-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - hooks/useReaderInitialization.ts
  - app/reader.tsx
  - hooks/useAutoSave.ts
  - stores/use-app-store.ts
  - __tests__/unit/hooks/useReaderInitialization-canonical.test.ts
  - __tests__/unit/lib/runtime-persistence.test.ts
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - PLAY-01
  - PLAY-02
  - PLAY-03
user_setup: []
must_haves:
  truths:
    - "Reader initialization can operate on canonical-first story/runtime snapshots"
    - "Autosave, manual save and load paths use one aligned story/runtime snapshot contract"
    - "Legacy local data still has a controlled fallback or migration path during the transition"
  artifacts:
    - path: hooks/useReaderInitialization.ts
      provides: Canonical-first reader initialization boundary
      contains: "runtime-story"
    - path: hooks/useAutoSave.ts
      provides: Autosave path aligned with canonical story/runtime snapshots
      contains: "runtime-story"
    - path: __tests__/unit/lib/runtime-persistence.test.ts
      provides: Regression coverage for save/load/autosave alignment
      min_lines: 30
  key_links:
    - from: app/reader.tsx
      to: hooks/useReaderInitialization.ts
      via: canonical-first initialization flow
      pattern: "useReaderInitialization"
    - from: stores/use-app-store.ts
      to: lib/runtime-story.ts
      via: save/load or snapshot helper usage
      pattern: "runtime-story"
---

<objective>
Align reader initialization, autosave and save/load persistence around the canonical runtime snapshot contract established in Wave 1.

Purpose: Ensure the user reads, saves and reloads the same scene structure that editor and preview now use.
Output: Canonical-first reader boundary, aligned persistence helpers, and focused regression coverage for save/load/autosave behavior.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-and-persistence-alignment/03-CONTEXT.md
@.planning/phases/03-runtime-and-persistence-alignment/03-01-PLAN.md
@hooks/useReaderInitialization.ts
@app/reader.tsx
@hooks/useAutoSave.ts
@stores/use-app-store.ts
@lib/runtime-story.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for canonical reader and persistence alignment</name>
  <files>__tests__/unit/hooks/useReaderInitialization-canonical.test.ts, __tests__/unit/lib/runtime-persistence.test.ts</files>
  <read_first>hooks/useReaderInitialization.ts, hooks/useAutoSave.ts, stores/use-app-store.ts, __tests__/unit/lib/story-hooks-canonical-scene.test.ts, __tests__/unit/use-reader-audio.test.ts</read_first>
  <action>Create focused tests that define the desired canonical-first behavior for reader initialization and persistence. At minimum, cover: initializing reader state from a canonical runtime story snapshot, autosave/manual save reading the same story contract, and loading a save without dropping canonical scene data. Verify these tests fail before implementation for the expected reason.</action>
  <verify>Run `pnpm test -- __tests__/unit/hooks/useReaderInitialization-canonical.test.ts __tests__/unit/lib/runtime-persistence.test.ts` and confirm failure is caused by missing alignment behavior, not by syntax or setup errors.</verify>
  <acceptance_criteria>
    - Both new test files exist
    - Tests fail before implementation
    - At least one test proves canonical persisted scenes survive a save/load round-trip
  </acceptance_criteria>
  <done>The desired reader and persistence alignment behavior is specified in executable tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor reader initialization to consume canonical runtime snapshots</name>
  <files>hooks/useReaderInitialization.ts, app/reader.tsx, lib/runtime-story.ts</files>
  <read_first>hooks/useReaderInitialization.ts, app/reader.tsx, lib/runtime-story.ts, lib/story-hooks.ts, .planning/phases/03-runtime-and-persistence-alignment/03-CONTEXT.md</read_first>
  <action>Update `useReaderInitialization.ts` and, if necessary, the narrow boundary in `app/reader.tsx` so reader initialization is driven by canonical-first runtime snapshots rather than assumptions tied only to compatibility `Story` reconstruction. Keep the UI behavior stable; Phase 3 is about data contract alignment, not reader redesign.</action>
  <verify>Run the new reader initialization test and the existing focused canonical tests after implementation.</verify>
  <acceptance_criteria>
    - `useReaderInitialization.ts` uses the new runtime helper layer
    - Reader initialization works when canonical scene records are present
    - No new legacy-only assumptions are introduced in `app/reader.tsx`
  </acceptance_criteria>
  <done>The reader starts from canonical-first runtime data instead of an implicit legacy-only contract.</done>
</task>

<task type="auto">
  <name>Task 3: Align autosave and save/load persistence with the canonical runtime contract</name>
  <files>hooks/useAutoSave.ts, stores/use-app-store.ts, lib/runtime-story.ts</files>
  <read_first>hooks/useAutoSave.ts, stores/use-app-store.ts, lib/runtime-story.ts, lib/story-state.ts, .planning/phases/02-editor-load-save-stability/02-02-SUMMARY.md</read_first>
  <action>Refactor autosave and any affected `saveGame` / `loadGame` boundaries in `stores/use-app-store.ts` so they operate on the same canonical-first runtime story contract used by reader initialization. Keep legacy fallback or migration behavior explicit and centralized; do not introduce another parallel compatibility path. If migration support is needed for local data, implement it as a named helper or documented fallback rather than scattered conditionals.</action>
  <verify>Run the persistence tests and existing focused canonical tests after implementation.</verify>
  <acceptance_criteria>
    - Autosave no longer depends on a legacy-only story path when canonical scenes exist
    - Save/load preserve canonical scene data through the runtime snapshot contract
    - Any local legacy fallback is explicit and centralized
  </acceptance_criteria>
  <done>Reader, autosave and save/load all share one aligned canonical-first runtime contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/hooks/useReaderInitialization-canonical.test.ts __tests__/unit/lib/runtime-persistence.test.ts` passes
- [ ] Existing Phase 1 and Phase 2 focused tests still pass
- [ ] Reader, autosave and save/load use one aligned runtime snapshot path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Reader initialization no longer depends on a legacy-only story contract
- Autosave and save/load preserve canonical scene data consistently
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-and-persistence-alignment/03-02-SUMMARY.md`
</output>
