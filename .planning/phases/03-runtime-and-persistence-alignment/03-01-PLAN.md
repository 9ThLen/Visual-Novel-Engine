---
phase: 03-runtime-and-persistence-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/runtime-story.ts
  - lib/story-hooks.ts
  - components/editor/PreviewScreen.tsx
  - __tests__/unit/lib/runtime-story.test.ts
  - __tests__/unit/lib/story-hooks-canonical-scene.test.ts
autonomous: true
requirements:
  - DATA-02
  - PLAY-01
  - PLAY-03
user_setup: []
must_haves:
  truths:
    - "Runtime-facing story and scene access is canonical-first and centralized in a pure helper layer"
    - "Preview can derive scene content from persisted canonical data instead of depending only on useEditorStore.timeline"
    - "Current story reconstruction and preview fallback remain backward-compatible for legacy data"
  artifacts:
    - path: lib/runtime-story.ts
      provides: Pure runtime snapshot and compatibility helpers for canonical scenes
      min_lines: 35
    - path: components/editor/PreviewScreen.tsx
      provides: Canonical-first preview data source
      contains: "runtime-story"
    - path: __tests__/unit/lib/runtime-story.test.ts
      provides: Coverage for canonical-first runtime snapshot behavior
      min_lines: 30
  key_links:
    - from: lib/story-hooks.ts
      to: lib/runtime-story.ts
      via: runtime story reconstruction helper
      pattern: "runtime-story"
    - from: components/editor/PreviewScreen.tsx
      to: lib/runtime-story.ts
      via: persisted preview scene resolution
      pattern: "runtime-story"
---

<objective>
Create a centralized canonical-first runtime snapshot layer and use it to align story reconstruction and preview behavior with persisted scene data.

Purpose: Stop preview and runtime-facing code from depending on ad hoc store reads or editor-only draft state.
Output: Pure runtime helper module, canonical-first preview path, and updated story reconstruction integration.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-and-persistence-alignment/03-CONTEXT.md
@lib/story-hooks.ts
@components/editor/PreviewScreen.tsx
@stores/use-app-store.ts
@lib/canonical-scene.ts
@lib/scene-record-adapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for canonical-first runtime snapshots</name>
  <files>__tests__/unit/lib/runtime-story.test.ts, __tests__/unit/lib/story-hooks-canonical-scene.test.ts</files>
  <read_first>__tests__/unit/lib/story-hooks-canonical-scene.test.ts, lib/story-state.ts, lib/canonical-scene.ts, stores/use-app-store.ts</read_first>
  <action>Before adding runtime helpers, create `__tests__/unit/lib/runtime-story.test.ts` that defines the expected canonical-first behavior for runtime story/scene snapshots. Cover at least: building a preview/runtime scene from a persisted `SceneRecord`, falling back to legacy data when canonical records are missing, and producing a compatibility shape that reader-facing code can consume. Extend `__tests__/unit/lib/story-hooks-canonical-scene.test.ts` if needed so story reconstruction still stays canonical-first after the refactor. Verify the new test fails before implementation.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/runtime-story.test.ts __tests__/unit/lib/story-hooks-canonical-scene.test.ts` and confirm the failure is caused by missing runtime helper behavior.</verify>
  <acceptance_criteria>
    - `__tests__/unit/lib/runtime-story.test.ts` exists
    - The new runtime test fails before implementation
    - At least one test proves canonical persisted scenes are preferred over legacy-only scene maps
  </acceptance_criteria>
  <done>The desired runtime snapshot behavior is captured in executable tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce a centralized runtime-story helper module</name>
  <files>lib/runtime-story.ts, lib/story-hooks.ts, stores/use-app-store.ts</files>
  <read_first>lib/story-hooks.ts, stores/use-app-store.ts, lib/scene-record-adapter.ts, lib/story-state.ts, .planning/phases/03-runtime-and-persistence-alignment/03-CONTEXT.md</read_first>
  <action>Add `lib/runtime-story.ts` with pure helpers for canonical-first runtime access, for example building a compatibility runtime story or preview scene from `SceneRecord` data with legacy fallback only when necessary. Refactor `lib/story-hooks.ts` to reuse this helper layer rather than maintaining a separate runtime-facing reconstruction path. Keep the API pure and avoid coupling it to React hooks or editor store state.</action>
  <verify>Run the runtime-story tests and existing story reconstruction tests after implementation.</verify>
  <acceptance_criteria>
    - `lib/runtime-story.ts` exists
    - `lib/story-hooks.ts` imports or uses the runtime helper layer
    - Runtime story reconstruction remains canonical-first and backward-compatible
  </acceptance_criteria>
  <done>The codebase has one centralized canonical-first helper layer for runtime-facing story and scene access.</done>
</task>

<task type="auto">
  <name>Task 3: Make PreviewScreen canonical-first with controlled editor-draft fallback</name>
  <files>components/editor/PreviewScreen.tsx, lib/runtime-story.ts</files>
  <read_first>components/editor/PreviewScreen.tsx, lib/runtime-story.ts, stores/use-app-store.ts, stores/use-editor-store.ts, .planning/phases/02-editor-load-save-stability/02-02-SUMMARY.md</read_first>
  <action>Refactor `PreviewScreen` so it can resolve preview content from persisted canonical scene data by `storyId/sceneId`, falling back to current `useEditorStore.timeline` only when previewing unsaved in-memory edits is necessary. Avoid a full UI rewrite; the goal is to align the data source, not redesign preview UX. Keep any fallback path explicit so the preview contract is understandable for later phases.</action>
  <verify>Run the runtime-story tests and add or extend a targeted assertion for preview scene resolution if needed.</verify>
  <acceptance_criteria>
    - `PreviewScreen` no longer relies only on `useEditorStore.timeline`
    - Persisted canonical scenes can be previewed by route params
    - Unsaved draft fallback remains explicit rather than implicit
  </acceptance_criteria>
  <done>Preview behavior aligns with the canonical persisted scene model while preserving a controlled draft fallback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/lib/runtime-story.test.ts __tests__/unit/lib/story-hooks-canonical-scene.test.ts` passes
- [ ] Existing Phase 1 and Phase 2 focused tests still pass
- [ ] Preview no longer depends solely on editor draft state for persisted scenes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Runtime-facing story/scene access is centralized and canonical-first
- Preview can read persisted canonical scene data
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-and-persistence-alignment/03-01-SUMMARY.md`
</output>
