---
phase: 18-audio-block-redesign
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - __tests__/unit/lib/audio-scene.test.ts
  - __tests__/unit/lib/audio-player-service.test.ts
  - __tests__/unit/lib/audio-manager-enhanced.test.ts
  - __tests__/unit/lib/reader-scene.test.ts
  - __tests__/unit/lib/reader-scene-cache.test.ts
  - __tests__/unit/lib/reader-runtime-snapshot.test.ts
requirements:
  - Regression-coverage
  - Migration-coverage
autonomous: true
---

<objective>
Close out remaining test coverage across files not already updated in
18-00 through 18-03, and run the full project verification gate end-to-end
for the phase.
</objective>

<tasks>

<task type="test">
  <name>Audio scene / player-service / manager coverage</name>
  <files>__tests__/unit/lib/audio-scene.test.ts __tests__/unit/lib/audio-player-service.test.ts __tests__/unit/lib/audio-manager-enhanced.test.ts</files>
  <action>
    Update `getAudioSceneMusicUri` (`lib/audio-scene.ts:13-18`) and its test
    to check `data.mode === 'track'` instead of `data.action === 'play'`. Add
    `crossFade` tests asserting both the outgoing track's volume ramps down
    over `fadeOutMs` and the incoming track's volume ramps up over
    `fadeInMs` (using the existing `_fade`/interval-based test approach
    already used for `play`/`stop` fade tests in this suite).
  </action>
</task>

<task type="test">
  <name>Reader scene / snapshot coverage</name>
  <files>__tests__/unit/lib/reader-scene.test.ts __tests__/unit/lib/reader-scene-cache.test.ts __tests__/unit/lib/reader-runtime-snapshot.test.ts</files>
  <action>
    Update any fixtures constructing `MusicBlockData`/`SoundBlockData` or
    `SceneState` music fields to the new shape. Confirm snapshot/cache tests
    don't assume the removed `musicAction`/`musicFadeDuration` fields exist.
  </action>
</task>

<task type="auto">
  <name>Full verification pass</name>
  <files></files>
  <action>
    Run the project's standard gate across the whole changed surface:
    `corepack pnpm run check`, the full unit test suite, and the web export
    smoke check. Fix any remaining compile/type errors surfaced by the
    contract change that weren't caught by the targeted test runs in
    18-00 through 18-03 (in particular, grep the repo for any remaining
    `.action` access on `MusicBlockData`/`SoundBlockData`-typed values, and
    any remaining `fadeDuration`/`musicFadeDuration`/`musicAction` reference
    outside of `lib/audio-block-migration.ts`'s legacy-input handling).
  </action>
  <acceptance_criteria>
    - `corepack pnpm run check` passes clean.
    - Full unit test suite passes.
    - `corepack pnpm exec node node_modules/expo/bin/cli export --platform web` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<rollback>
None needed beyond what 18-00 through 18-03 already define; this plan is
test-only plus a final verification sweep.
</rollback>

<verification>
- `corepack pnpm run check`
- `corepack pnpm test`
- `corepack pnpm exec node node_modules/expo/bin/cli export --platform web`
- `gsd-code-review` on all files touched across 18-00 through 18-04
</verification>

<success_criteria>
- Entire test suite is green with no references to the removed
  `action`/`fadeDuration`/`musicAction`/`pause` fields anywhere in
  non-migration code.
- Phase 18 objective from `18-CONTEXT.md` is fully realized: no action
  dropdown, explicit fade in/out, explicit scene-vs-continuous binding,
  optional auto-fade, real crossfade, canonical migration.
</success_criteria>
