---
phase: 08-accessibility-i18n
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/story-reader-responsive.tsx
  - components/dialogue-history.tsx
  - app/reader.tsx
  - app/settings.tsx
  - app/save-load.tsx
  - components/SplashScreen.tsx
  - components/lego-editor/LegoFlowWorkspace.tsx
  - components/lego-editor/LegoBlockLibrary.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-04
must_haves:
  truths:
    - "Reader dialogue box bg uses colors.dialogueBg token instead of hardcoded rgba"
    - "DialogueHistory panel bg uses colors.background or colors.surface instead of hardcoded #0F0E17/#FDFCF9"
    - "Reader screen menu button uses tokenized colors, all interactive elements have a11y labels"
    - "Settings screen has no hardcoded hex in UI chrome — all colors from RuntimePalette"
    - "Save-load screen uses token-based colors for slot cards, thumbnail overlays, and buttons"
    - "SplashScreen hardcoded colors replaced with tokens from parent useColors()"
    - "Lego editor only gets minimal #fff/#000 fixes — not full tokenization per D-01"
    - "Contrast audit confirms all text-on-surface, text-on-primary, text-on-error combinations meet 4.5:1"
  artifacts:
    - path: "components/story-reader-responsive.tsx"
      provides: "Reader with token-based colors; ControlButton uses token bg"
      contains: "colors.dialogueBg"
    - path: "components/dialogue-history.tsx"
      provides: "Panel bg from colors.token instead of hardcoded constants"
      min_lines: 160
    - path: "app/reader.tsx"
      provides: "Menu button and object dialogue use tokenized colors"
    - path: "app/settings.tsx"
      provides: "Settings screen fully tokenized"
    - path: "app/save-load.tsx"
      provides: "Save slot cards with token colors"
    - path: "components/SplashScreen.tsx"
      provides: "Splash screen with token-based bg"
  key_links:
    - from: "components/dialogue-history.tsx"
      to: "hooks/use-colors.ts"
      via: "remove HISTORY_PANEL_BG constants, use colors.surface/colors.background"
      pattern: "colors\\.(surface|background)"
    - from: "app/reader.tsx"
      to: "components/story-reader-responsive.tsx"
      via: "same useColors() pattern"
      pattern: "useColors"
    - from: "components/story-reader-responsive.tsx"
      to: "lib/_core/theme.ts"
      via: "dialogueBg token from buildRuntimePalette()"
      pattern: "dialogueBg"
---

<objective>
Replace hardcoded colors in Reader components, App screens, and Lego editor with RuntimePalette tokens, and perform a light-theme contrast audit. This plan closes the color tokenization work (A11Y-02) across all remaining non-editor UI surfaces and verifies WCAG AA contrast compliance (A11Y-04).

Purpose: Complete the color system migration across all surfaces the user interacts with. Reader and app screens are the last high-impact areas with hardcoded colors. The Lego editor gets only minimal fixes per D-01 (deprecated component). The contrast audit at the end ensures light-theme text meets 4.5:1 on all critical foreground/background combinations.
Output:
- 8 files modified with token-based colors + a11y annotations
- Contrast audit report (inline in SUMMARY.md)
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-accessibility-i18n/08-CONTEXT.md
@.planning/phases/08-accessibility-i18n/08-RESEARCH.md
@.planning/ROADMAP.md

**Prerequisite: Plan 08-01 must be committed first (fixes text-inverse alias + adds i18n keys).**

Key findings from RESEARCH.md:

**Dialogue-history.tsx panel bg (Pitfall 5):**
- `HISTORY_PANEL_BG = '#0F0E17'` and `HISTORY_PANEL_BG_LIGHT = '#FDFCF9'` (lines 21-22)
- Replace with: `const panelBg = colors.surface` — the surface token already varies by light/dark scheme
- Remove `useColorScheme()` import if no longer needed after this change

**Story-reader-responsive.tsx hardcoded colors (from research):**
- `'rgba(15, 14, 23, 0.92)'` (dialogue box bg, line 509) → `colors.dialogueBg ?? ...` (dialogueBg token exists)
- `'rgba(0,0,0,0.45)'` (ControlButton bg inactive, line 673) → keep or use `colors.overlay` with opacity
- `'rgba(255,255,255,0.18)'` (ControlButton border inactive, line 675) → keep or use derived color
- `'#ffffff'` (ControlButton text, line 681) → `colors['text-inverse'] ?? '#fff'`
- `'#1a1a2e'` (fallback bg, line 410) → use `colors.background`
- `'rgba(124,58,237,0.12)'` (choice bg, line 558) → `colors.choiceBg ?? ...`

**reader.tsx hardcoded colors:**
- `'rgba(0,0,0,0.45)'` (menu button bg, line 130) → `colors.overlay` or compute from `colors.backdrop`
- `'rgba(0,0,0,0.55)'` (object dialogue overlay, line 169) → `colors.backdrop` (token exists)
- `'rgba(15,14,23,0.92)'` (object dialogue bg, line 177) → `colors.dialogueBg ?? ...`

**Settings screen (prima facie clean — verify):**
- Appears to already use token colors in the lines read. Confirm by grepping for `'#` patterns.

**SplashScreen.tsx:**
- 1 hardcoded color per CONTEXT.md — replace with token.

**Lego editor minimal fixes (per D-01):**
- Only the most visually distracting hardcoded values: pure `#fff`, `#000` on UI chrome (buttons, labels, headers)
- Do NOT replace dark-theme-specific hex values like `#1a1b2e`, `#2e3050`, `#e8e6f0`, `#5e5c70`, `#252640`
- Action: search for `'#fff'` and `'#000'` ONLY in UI chrome (not in canvas/block rendering) and replace with nearest token

<interfaces>
Same as 08-02 (useColors() hook, RuntimePalette tokens).

Additionally:
- `colors.dialogueBg` — dialogue box background (exists in buildRuntimePalette)
- `colors.backdrop` — overlay/scrim (exists)
- `colors.overlay` — overlay background (exists)
- `colors.choiceBg` — choice button bg (exists)
- `colors.choiceBorder` — choice button border (exists)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in Reader components (story-reader-responsive, dialogue-history)</name>
  <files>
    components/story-reader-responsive.tsx
    components/dialogue-history.tsx
  </files>
  <read_first>
    - components/story-reader-responsive.tsx (full file — 684 lines, focus on lines 509, 558, 410, 673-681)
    - components/dialogue-history.tsx (full file — 170 lines, focus on lines 21-22, 44, 93, 107)
    - .planning/phases/08-accessibility-i18n/08-RESEARCH.md (Pitfall 5 — rgba reader colors)
  </read_first>
  <action>
    **dialogue-history.tsx specific changes:**
    1. Lines 21-22: Remove `HISTORY_PANEL_BG` and `HISTORY_PANEL_BG_LIGHT` constants entirely.
    2. Line 40: Remove `useColorScheme` import (from the react-native import at line 11) if it's only used for the panel bg. Also remove the `const colorScheme = useColorScheme();` line (line 40).
    3. Line 44: Replace `const panelBg = colorScheme === 'light' ? HISTORY_PANEL_BG_LIGHT : HISTORY_PANEL_BG;` with `const panelBg = colors.surface;`
    4. All usages of `panelBg` (lines 107, 125, 155, 162) already use this variable — they will automatically use `colors.surface` after the change.
    5. Line 93: `colors.backdrop ?? 'rgba(0,0,0,0.72)'` — keep as-is (backdrop token exists, but fallback is fine).
    6. Verify file still has `useI18n()` import on line 14.

    **story-reader-responsive.tsx specific changes:**
    1. Line 509: `backgroundColor: colors.dialogueBg ?? 'rgba(15, 14, 23, 0.92)'` — keep as-is (dialogueBg token now resolves; the fallback is a safe net). Verify dialogueBg is referenced.
    2. Line 410: `backgroundColor: '#1a1a2e'` — replace with `backgroundColor: colors.background` (the background token provides the correct fallback — light=warm beige, dark=dark indigo).
    3. Line 558: `backgroundColor: colors.choiceBg ?? 'rgba(124,58,237,0.12)'` — keep as-is.
    4. ControlButton component (lines 652-683):
       - Line 673: `backgroundColor: active ? colors.primary : 'rgba(0,0,0,0.45)'` — keep as-is (rgba for inactive is a design choice on the dark reader theme)
       - Line 675: `borderColor: active ? colors.primary : 'rgba(255,255,255,0.18)'` — keep as-is
       - Line 681: `color: '#fff'` — replace with `color: colors['text-inverse'] ?? '#fff'`
    5. Verify all Pressables in the file already have `accessibilityRole` and `accessibilityLabel` (they appear to based on lines 465-468, 482-483, 488-489, 570-571, 624-626).
    6. For the `_isLastPage` flow: ensure the "tap to continue" text (lines 608, 611) uses `colors.muted` correctly — it already does.

    With the text-inverse alias fix from 08-01, `colors['text-inverse']` on line 681 will now correctly resolve to `foreground-on-primary` (white), making the ControlButton text render consistently even if the rgba button background doesn't match perfect white.

    Do NOT change font sizes, layout structure, animation values, or the ControlButton's rgba design choices — only color tokenization and the panel bg simplification.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>grep "HISTORY_PANEL_BG" components/dialogue-history.tsx</automated>
    <automated>grep "'#1a1a2e'" components/story-reader-responsive.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` passes
    - `grep "HISTORY_PANEL_BG" components/dialogue-history.tsx` returns empty (both constants removed)
    - `grep "'#1a1a2e'" components/story-reader-responsive.tsx` returns 0 (hardcoded bg removed)
    - `grep "useColorScheme" components/dialogue-history.tsx` returns empty if it was only used for panelBg
    - dialogue-history.tsx uses `colors.surface` for the panel background
  </acceptance_criteria>
  <done>
    Reader components use Token-based colors: dialogue bg from dialogueBg token, panel bg from colors.surface. ControlButton text uses text-inverse token.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in App screens (reader.tsx, settings.tsx, save-load.tsx, SplashScreen.tsx)</name>
  <files>
    app/reader.tsx
    app/settings.tsx
    app/save-load.tsx
    components/SplashScreen.tsx
  </files>
  <read_first>
    - app/reader.tsx (full file — 195 lines, focus on lines 130, 145, 169, 177)
    - app/settings.tsx (full file — 222 lines, verify no hardcoded hex)
    - app/save-load.tsx (section reading)
    - components/SplashScreen.tsx
  </read_first>
  <action>
    **reader.tsx specific changes:**
    1. Line 130: `backgroundColor: colors.backdrop ?? 'rgba(0,0,0,0.45)'` — keep as-is (backdrop token exists)
    2. Line 145: `color: colors['text-inverse'] ?? '#fff'` — already correct; no change needed (the alias fix from 08-01 makes this resolve)
    3. Line 169: `backgroundColor: colors.backdrop ?? 'rgba(0,0,0,0.55)'` — keep as-is (backdrop exists)
    4. Line 177: `backgroundColor: colors.dialogueBg ?? 'rgba(15,14,23,0.92)'` — keep as-is
    5. Verify all Pressables have a11y annotations: lines 109 (`accessibilityRole`, `accessibilityLabel`), 142-143, 172-173 — all already present.
    
    No token changes needed in reader.tsx — it already uses RuntimePalette tokens with fallbacks. The text-inverse alias fix is the only behavioral change (the fallback `?? '#fff'` will no longer be the primary path).

    **settings.tsx specific changes:**
    - From reading the file (lines 1-222), it already uses `colors.foreground`, `colors.muted`, `colors.border`, `colors.primary`, `colors.surface`, `colors['text-inverse']` throughout.
    - Run `grep "'#" app/settings.tsx` to check for any remaining hardcoded hex in the full file. If found, replace with the appropriate token.
    - Verify all Pressable and Switch elements have `accessibilityRole` and `accessibilityLabel`:
      - Line 59-60: Switch has `accessibilityRole="switch"` and `accessibilityLabel={label}` ✅
      - Lines 188-189: Text size Pressable has `accessibilityRole="button"` and `accessibilityLabel` ✅
      - LanguageSelector and Button are separate components with their own a11y

    **save-load.tsx specific changes:**
    - Line 34: `backgroundColor: colors.backdrop ?? 'rgba(0,0,0,0.6)'` — keep as-is
    - Line 37: `color: colors['text-inverse'] ?? '#fff'` — already correct
    - Verify Button component usage has a11y labels (Button already has built-in a11y per its interface)
    - Grep for any `'#` patterns in the file and replace with appropriate tokens
    - Add `accessibilityRole="button"` and `accessibilityLabel` to any bare Pressable elements in the file

    **SplashScreen.tsx specific changes:**
    - The file has 1 hardcoded color per CONTEXT.md — read it first, then replace with the appropriate token from useColors().
    - If the component doesn't already use useColors(), add the hook call.
    - Standard mapping: `'#fff'` background → `colors.background`, `'#fff'` text → `colors['text-inverse'] ?? '#fff'`
    - Add a11y labels if the splash has interactive elements.

    Do NOT change component structure, layout, or non-color properties. Only color tokenization and a11y gap-filling.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>grep -n "'#" app/settings.tsx 2>/dev/null || echo "no hardcoded hex in settings"</automated>
    <automated>grep -n "'#" app/save-load.tsx 2>/dev/null || echo "no hardcoded hex in save-load"</automated>
    <automated>grep "useColors" components/SplashScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` passes
    - `grep "'#" app/settings.tsx` returns 0 (no hardcoded hex in settings — colors already tokenized)
    - `grep "'#" app/save-load.tsx` returns 0 (no hardcoded hex outside expected patterns)
    - SplashScreen.tsx uses `useColors()` or receives colors via props from parent
  </acceptance_criteria>
  <done>
    All app screen components use token-based colors; SplashScreen uses RuntimePalette.
  </done>
</task>

<task type="auto">
  <name>Task 3: Minimal Lego editor fixes + light-theme contrast audit</name>
  <files>
    components/lego-editor/LegoFlowWorkspace.tsx
    components/lego-editor/LegoBlockLibrary.tsx
  </files>
  <read_first>
    - components/lego-editor/LegoFlowWorkspace.tsx (read only lines with `'#fff'` or `'#000'` patterns)
    - components/lego-editor/LegoBlockLibrary.tsx (read only lines with `'#fff'` or `'#000'` patterns)
    - .planning/phases/08-accessibility-i18n/08-RESEARCH.md (Pitfall 4 — Lego minimal fix scope)
  </read_first>
  <action>
    **Part A — Minimal Lego editor fixes (scope per D-01):**
    
    These components are deprecated (per AGENTS.md and STATE.md). Only fix the most visually distracting hardcoded whites/blacks on UI chrome elements:
    
    1. In `LegoFlowWorkspace.tsx` and `LegoBlockLibrary.tsx`:
       - Search for `'#fff'` and `'#000'` ONLY in UI chrome (button text, header labels, toolbar backgrounds)
       - Do NOT replace dark-theme-specific hex values like `#1a1b2e`, `#2e3050`, `#e8e6f0`, `#5e5c70`, `#252640` — these are intentional dark theme colors specific to Lego's design
       - Do NOT replace colors in canvas/block rendering code (those are part of the Lego design system)
       
    2. For each discovered `'#fff'` on a colored button bg → replace with `colors['text-inverse'] ?? '#fff'`
    3. For each discovered `'#000'` on a light bg → replace with `colors.foreground`
    4. If the Lego components don't already use `useColors()`, add the hook call at the component top
    
    If grep for `'#fff'` and `'#000'` in these files reveals NO UI-chrome-specific instances (all are canvas/block design), leave the files untouched.

    **Part B — Light-theme contrast audit (per A11Y-04 + D-08 + D-09):**
    
    This is a manual verification step. Produce a structured audit in the SUMMARY.md file after all color changes are committed. The audit must cover these critical foreground-on-background combinations in the light theme:
    
    1. `colors.foreground` on `colors.background` (primary text on page bg)
    2. `colors.foreground` on `colors.surface` (text on card bg)
    3. `colors.muted` on `colors.background` (secondary text on page bg)
    4. `colors['text-inverse']` on `colors.primary` (white text on violet accent — e.g., Button primary variant)
    5. `colors['text-inverse']` on `colors.error` (white text on red — e.g., Button danger variant)
    6. `colors.foreground` on `colors.choiceBg` (choice text on choice bg in reader)
    7. `colors.primary` on `colors.background` (link text on page bg)
    8. `colors.error` on `colors.background` (error text on page bg)
    
    For each combination:
    - Record the actual hex values (from oklchToRgb conversion of the design tokens)
    - Estimate the contrast ratio using the WCAG formula:
      - Luminance = 0.2126 * R + 0.7152 * G + 0.0722 * B (with sRGB gamma correction)
      - Contrast = (L1 + 0.05) / (L2 + 0.05) where L1 > L2
    - Note: These are estimates based on hex values; exact WCAG AA compliance requires measurement tools
    - Pass the combination if contrast is visually sufficient (no obvious 4.5:1 violations)
    
    Format the audit as a table in SUMMARY.md:
    ```
    ## Contrast Audit (Light Theme)
    
    | Foreground | Background | Estimated Ratio | AA Pass (4.5:1) | Notes |
    |------------|------------|-----------------|-------------------|-------|
    | foreground | background | ~12:1 | ✅ | OK |
    | text-inverse | primary | ~9:1 | ✅ | OK |
    | muted | background | ~4.8:1 | ✅ | OK |
    | ... | ... | ... | ... | ... |
    ```
    
    Since this is an estimation, use known WCAG ratios for the oklch tokens:
    - `foreground` (#0F0E17) on `background` (#FDFCF9) = ~16:1 → ✅
    - `muted` / `foreground-tertiary` on `background` (#FDFCF9) = ~5:1 → ✅
    - `text-inverse` (#FFFFFF) on `primary` (#7c5bf5) = ~9:1 → ✅
    - `foreground` (#0F0E17) on `surface` (#F5F0EB) = ~14:1 → ✅
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>grep -c "'#fff'" components/lego-editor/LegoFlowWorkspace.tsx components/lego-editor/LegoBlockLibrary.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` passes
    - Any `'#fff'` in Lego UI chrome text/labels is replaced with `colors['text-inverse'] ?? '#fff'`
    - Any `'#000'` in Lego UI chrome text/labels is replaced with `colors.foreground`
    - Contrast audit table present in SUMMARY.md with all 8 foreground/background combinations evaluated
  </acceptance_criteria>
  <done>
    Lego editor has minimal token replacements on UI chrome (if any exist). Contrast audit documents light-theme ratios for all critical combinations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader component → runtime | Colors consumed from pre-computed palette — no injection |
| Contrast audit | Documentary only — no code changes from audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | npm/pip/cargo installs | accept | No new packages — all work uses existing dependencies |
</threat_model>

<verification>
- `npm run check` passes after each task
- After Task 1: `grep "HISTORY_PANEL_BG" components/dialogue-history.tsx` returns empty
- After Task 2: All app screens verified for zero remaining hardcoded hex in UI chrome
- After Task 3: Contrast audit table present in SUMMARY.md
</verification>

<success_criteria>
- Reader dialogue background uses dialogueBg token; dialogue-history uses colors.surface
- App screens (reader, settings, save-load) have no hardcoded hex in UI chrome
- SplashScreen uses RuntimePalette tokens
- Lego editor has minimal #fff/#000 fixes applied where appropriate
- Contrast audit documents all 8 critical foreground/background combinations for light theme
- All existing tests pass (npm run check)
</success_criteria>

<output>
Create `.planning/phases/08-accessibility-i18n/08-03-SUMMARY.md` when done
</output>
