---
phase: 08-accessibility-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/_core/theme.ts
  - lib/translations.ts
  - components/ErrorBoundary.tsx
autonomous: true
requirements:
  - A11Y-02
  - A11Y-03
must_haves:
  truths:
    - "colors['text-inverse'] resolves to a valid hex string instead of undefined"
    - "The ErrorBoundary component no longer exposes a `colors` prop in its public interface"
    - "All editor toolbar actions, block labels, confirmation messages, and a11y verbs have i18n keys in en/uk/pl"
    - "New i18n keys do not conflict with existing keys"
  artifacts:
    - path: "lib/_core/theme.ts"
      provides: "buildRuntimePalette() with text-inverse alias"
      contains: "text-inverse"
    - path: "lib/translations.ts"
      provides: "new i18n keys for editor toolbar, block actions, confirmation strings, a11y verbs"
      min_lines: 520
    - path: "components/ErrorBoundary.tsx"
      provides: "ErrorBoundaryProps without optional colors prop; internal class kept for componentDidCatch"
      contains: "export function ErrorBoundary"
  key_links:
    - from: "lib/_core/theme.ts"
      to: "RuntimePalette type"
      via: "text-inverse alias maps foreground-on-primary"
      pattern: "'text-inverse'"
    - from: "lib/translations.ts"
      to: "allTranslations export"
      via: "new keys added to all 3 language dictionaries"
      pattern: "t\\('"
    - from: "components/ErrorBoundary.tsx"
      to: "lib/_core/theme.ts"
      via: "useColors() provides typed palette"
      pattern: "useColors"
---

<objective>
Infrastructure foundation for Phase 8. Fix the `text-inverse` color alias so that all components resolve it correctly, add ~30 new i18n keys for editor toolbar actions, block labels, confirmation strings, and a11y verbs, and clean up the ErrorBoundary component's public API so `colors` prop is no longer exposed (per D-02).

Purpose: Eliminate the root cause of all `colors['text-inverse'] ?? '#fff'` fallbacks being triggered, provide i18n key coverage for downstream tasks, and prepare ErrorBoundary for theme-native rendering — all before any per-component color or a11y work begins.
Output:
- `lib/_core/theme.ts` — text-inverse alias added to buildRuntimePalette()
- `lib/translations.ts` — ~30 new keys across en/uk/pl dictionaries
- `components/ErrorBoundary.tsx` — colors prop removed from public interface, internal class kept for componentDidCatch
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-accessibility-i18n/08-CONTEXT.md
@.planning/phases/08-accessibility-i18n/08-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From lib/_core/theme.ts:
- `buildRuntimePalette(scheme: ColorScheme): RuntimePalette` — line 118, returns object with aliases
- `RuntimePalette` type — line 74, includes `[key: string]: string | undefined` index signature
- `Colors.light / Colors.dark` — line 157, constructed via buildRuntimePalette
- Current aliases (lines 125-133): text, tint, icon, tabIconDefault, tabIconSelected, muted, error, surfaceElevated, dialogueBg, nameBg, nameText, choiceBg, choiceBorder, choiceHover, overlay, scrim, backdrop, editorRuler, secondary, hover, surface-container, surface-1, surface-2

From lib/translations.ts:
- `EN: Record<string, string>` — line 21, English dictionary
- `UK: Record<string, string>` — line 173, Ukrainian dictionary  
- `PL: Record<string, string>` — line 325, Polish dictionary (partial coverage)
- `allTranslations: Record<Language, Record<string, string>>` — line 472

From components/ErrorBoundary.tsx:
- `ErrorBoundaryProps` — line 8, optional `colors?: RuntimePalette` must be removed
- `ErrorBoundary` (FC) — line 166, calls useColors() and useI18n(), passes to ErrorBoundaryInner
- `ErrorBoundaryInner` (class) — line 27, implements componentDidCatch, renders error UI
- Line 149: `colors['text-inverse'] ?? '#fff'` — will resolve correctly after text-inverse fix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix text-inverse alias in buildRuntimePalette()</name>
  <files>lib/_core/theme.ts</files>
  <read_first>
    - lib/_core/theme.ts (the buildRuntimePalette function, lines 118-154)
    - lib/_core/theme.ts (RuntimePalette type, lines 74-116 — note the index signature)
  </read_first>
  <action>
    In `buildRuntimePalette()` (around line 132, after the `error: base.danger` alias), add:
    `'text-inverse': base['foreground-on-primary'] || base['foreground-inverse'] || '#FFFFFF'`
    
    This must be placed within the return object, alongside the existing aliases like `text`, `tint`, `icon`, `muted`, `error`, `surfaceElevated`. The `base` object already contains `foreground-on-primary` (sourced from `theme-colors.json` via `buildSchemePalette` and spread via `...base` on line 123).

    Do NOT change the RuntimePalette type — its `[key: string]: string | undefined` index signature already supports bracket access for `'text-inverse'`.
  </action>
  <verify>
    <automated>grep -n "'text-inverse'" lib/_core/theme.ts | grep -v "//\|/\*"</automated>
    <automated>node -e "const t = require('./lib/_core/theme'); const p = t.Colors.light; console.assert(p['text-inverse'] !== undefined, 'text-inverse alias missing'); console.log('OK:', p['text-inverse']);"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `'text-inverse'` in `lib/_core/theme.ts` finds the alias in the return object of `buildRuntimePalette()`
    - Node assertion confirms `Colors.light['text-inverse']` returns a non-undefined string (hex or rgb)
    - `npm run check` passes with no new type errors
  </acceptance_criteria>
  <done>
    text-inverse alias exists in buildRuntimePalette() return value and resolves to a valid color string at runtime.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ~30 new i18n keys to translations.ts</name>
  <files>lib/translations.ts</files>
  <read_first>
    - lib/translations.ts (full file — understand the key naming patterns, existing categories, and PL coverage gaps)
    - .planning/phases/08-accessibility-i18n/08-RESEARCH.md (section on ~30 new keys needed)
  </read_first>
  <action>
    Add approximately 30 new i18n keys to the EN, UK, and PL dictionaries in `lib/translations.ts`. The keys must be organized in the same alphabetical/category pattern as the existing keys.

    Categories and suggested key names (CONCRETE list — add every one of these):

    **Editor toolbar / block actions:**
    - `'editor.duplicate'` — EN: "Duplicate", UK: "Дублювати", PL: "Duplikuj"
    - `'editor.properties'` — EN: "Properties", UK: "Властивості", PL: "Właściwości"
    - `'editor.noBlockSelected'` — EN: "No block selected", UK: "Блок не вибрано", PL: "Nie wybrano bloku"
    - `'editor.confirmDeleteSceneTitle'` — EN: "Delete Scene", UK: "Видалити сцену", PL: "Usuń scenę"
    - `'editor.confirmDeleteSceneMessage'` — EN: "Are you sure you want to delete this scene? This cannot be undone.", UK: "Ви впевнені, що хочете видалити цю сцену? Цю дію не можна скасувати.", PL: "Czy na pewno chcesz usunąć tę scenę? Tej operacji nie można cofnąć."
    - `'editor.confirmDeleteStoryMessage'` — EN: "Are you sure you want to delete this story? This cannot be undone.", UK: "Ви впевнені, що хочете видалити цю історію? Цю дію не можна скасувати.", PL: "Czy na pewno chcesz usunąć tę historię? Tej operacji nie można cofnąć."

    **Common (existing category, new keys):**
    - `'common.save'` — EN: "Save", UK: "Зберегти", PL: "Zapisz"
    - `'common.done'` — EN: "Done", UK: "Готово", PL: "Gotowe"
    - `'common.edit'` — EN: "Edit", UK: "Редагувати", PL: "Edytuj"
    - `'common.rename'` — EN: "Rename", UK: "Перейменувати", PL: "Zmień nazwę"
    - `'common.copy'` — EN: "Copy", UK: "Копіювати", PL: "Kopiuj"
    - `'common.paste'` — EN: "Paste", UK: "Вставити", PL: "Wklej"
    - `'common.select'` — EN: "Select", UK: "Вибрати", PL: "Wybierz"

    **Settings (new keys):**
    - `'settings.accessibility'` — EN: "Accessibility", UK: "Доступність", PL: "Dostępność"
    - `'settings.reduceMotion'` — EN: "Reduce Motion", UK: "Зменшити рух", PL: "Ogranicz ruch"

    **Save/Load (new keys):**
    - `'save.autoSaveLabel'` — EN: "Autosave", UK: "Автозбереження", PL: "Autozapis"
    - `'save.manualSaves'` — EN: "Manual Saves", UK: "Ручні збереження", PL: "Ręczne zapisy"
    - `'save.noSaves'` — EN: "No saves yet", UK: "Ще немає збережень", PL: "Brak zapisów"

    **Accessibility / a11y verbs (new section, prefix `a11y`):**
    - `'a11y.closePanel'` — EN: "Close panel", UK: "Закрити панель", PL: "Zamknij panel"
    - `'a11y.openMenu'` — EN: "Open menu", UK: "Відкрити меню", PL: "Otwórz menu"
    - `'a11y.closeMenu'` — EN: "Close menu", UK: "Закрити меню", PL: "Zamknij menu"
    - `'a11y.dragBlock'` — EN: "Drag to reorder", UK: "Перетягніть для зміни порядку", PL: "Przeciągnij, aby zmienić kolejność"
    - `'a11y.selectBlock'` — EN: "Select block", UK: "Вибрати блок", PL: "Wybierz blok"
    - `'a11y.addBlock'` — EN: "Add new block", UK: "Додати новий блок", PL: "Dodaj nowy blok"
    - `'a11y.deleteBlock'` — EN: "Delete block", UK: "Видалити блок", PL: "Usuń blok"

    **Editor additional keys:**
    - `'editor.moveUp'` — EN: "Move Up", UK: "Перемістити вгору", PL: "Przesuń w górę"
    - `'editor.moveDown'` — EN: "Move Down", UK: "Перемістити вниз", PL: "Przesuń w dół"
    - `'editor.blockActions'` — EN: "Block Actions", UK: "Дії з блоком", PL: "Akcje bloku"
    - `'editor.sceneProperties'` — EN: "Scene Properties", UK: "Властивості сцени", PL: "Właściwości sceny"
    - `'editor.selectBlockType'` — EN: "Select block type", UK: "Виберіть тип блоку", PL: "Wybierz typ bloku"

    **Reader (missing keys):**
    - `'reader.logTitle'` — EN: "Dialogue History", UK: "Історія діалогу", PL: "Historia dialogów"
    - `'reader.noFurtherContent'` — EN: "No further content", UK: "Немає подальшого вмісту", PL: "Brak dalszej treści"

    Add all of the above to EN (line 21), UK (line 173), and PL (line 325) dictionaries in alphabetical order within their category section. For PL, UK values cannot be used as PL — provide proper Polish translations. Each key must appear in all three dictionaries (even if PL coverage is partial, these new keys must be present).

    Maintain the existing dictionary structure (no new sections or formatting changes).
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node -e "
const t = require('./lib/translations.ts');
const assert = require('assert');
// Can't require TS directly, but grep proves they exist
" 2>/dev/null; echo "Keys check with grep:"; grep -c "'a11y." lib/translations.ts; grep -c "'editor.duplicate'" lib/translations.ts
</automated>
  </verify>
  <acceptance_criteria>
    - All 30+ new keys exist in EN, UK, and PL dictionaries (verified by grep for each category prefix)
    - No duplicate keys (existing keys like `'common.save'` were previously missing — grep confirms no conflict)
    - Every new PL key is a genuine Polish translation, not copied from UK
    - `npm run check` passes with no TypeScript errors
  </acceptance_criteria>
  <done>
    Translation dictionaries in lib/translations.ts contain all new keys for editor toolbar, block actions, confirmation strings, a11y verbs, settings, and save/load categories in all three languages.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up ErrorBoundary public API — remove colors prop</name>
  <files>components/ErrorBoundary.tsx</files>
  <read_first>
    - components/ErrorBoundary.tsx (full file — understand the FC + inner class architecture)
    - .planning/phases/08-accessibility-i18n/08-RESEARCH.md (ErrorBoundary FC rewrite section)
  </read_first>
  <action>
    Per D-02, make the `ErrorBoundary` component's public API not expose a `colors` prop. The functional wrapper (`ErrorBoundary`) already calls `useColors()` and `useI18n()` and passes their values to the inner class (`ErrorBoundaryInner`). The cleanup is minimal:

    1. In `ErrorBoundaryProps` (line 8-12): Remove the optional `colors?: RuntimePalette` field. The remaining fields are `children: ReactNode` and `fallback?: ...`. Also remove the `import type { RuntimePalette } from '@/lib/_core/theme';` since it is no longer referenced in the props interface.

    2. In `ErrorBoundaryInnerProps` (line 14-19): Keep `colors: RuntimePalette` — this is the internal class that still receives colors from the parent FC.

    3. In the exported `ErrorBoundary` function (line 166-180): The function signature destructures `{ children, colors, fallback }` — change to `{ children, fallback }`. Remove the `colors` variable. The usage `colors ?? palette` on line 172 changes to just `palette`.

    4. Keep `ErrorBoundaryInner` (class component) exactly as-is — it needs `componentDidCatch` for React error boundary behavior. No rendering changes.

    After this change, any external usage of `<ErrorBoundary colors={...}>` in other files (e.g., `app/scene-editor.tsx` where Phase 7 wrapped it) will cause a TypeScript error if they pass `colors`. Search for `<ErrorBoundary` usage across the codebase and update any call site that passes the `colors` prop by removing that prop. The Phase 7 wrap at `app/scene-editor.tsx` likely does NOT pass `colors` (research confirms: "the `colors` prop is never passed from outside"), but verify by grep.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>grep -n "colors?" components/ErrorBoundary.tsx | grep -v "//\|colors\.\|colors\['\|ErrorBoundaryInner\|InnerProps"</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorBoundaryProps` no longer has a `colors` field (grep for `colors?:` in the interface returns empty)
    - `ErrorBoundary` function signature no longer destructures `colors`
    - `ErrorBoundaryInner` class still receives `colors` via its props (internal only)
    - No call site in the codebase passes a `colors` prop to `<ErrorBoundary>`
    - `npm run check` passes without type errors
  </acceptance_criteria>
  <done>
    ErrorBoundary public API no longer exposes colors prop; the component uses useColors() internally per D-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ErrorBoundary → children | Error boundary catches rendering errors from child components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | npm/pip/cargo installs | accept | No new packages — all work uses existing dependencies |
</threat_model>

<verification>
- `npm run check` passes after each task
- After Task 1: `console.assert(Colors.light['text-inverse'] !== undefined)` passes
- After Task 2: No duplicate i18n keys, all new keys present in EN/UK/PL
- After Task 3: ErrorBoundaryProps interface no longer has colors field
</verification>

<success_criteria>
- text-inverse alias exists in buildRuntimePalette() and resolves correctly
- ~30 new i18n keys exist across all three language dictionaries
- ErrorBoundary public API cleaned up per D-02
- all existing tests pass (npm run check)
</success_criteria>

<output>
Create `.planning/phases/08-accessibility-i18n/08-01-SUMMARY.md` when done
</output>
