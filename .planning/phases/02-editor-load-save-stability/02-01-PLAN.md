---
phase: 02-editor-load-save-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/editor-scene-draft.ts
  - stores/use-editor-store.ts
  - app/scene-editor.tsx
  - components/editor/SceneComposer.tsx
  - __tests__/unit/lib/editor-scene-draft.test.ts
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
user_setup: []
must_haves:
  truths:
    - "Opening an existing scene hydrates the editor draft from the canonical persisted record"
    - "Editor route can open a scene without relying on SceneManager having preloaded useEditorStore"
    - "SceneComposer no longer resets an existing scene to an empty timeline during mount"
  artifacts:
    - path: lib/editor-scene-draft.ts
      provides: Pure helpers for converting persisted canonical scene data into editor draft state
      min_lines: 25
    - path: app/scene-editor.tsx
      provides: Canonical route-level scene hydration
      contains: "selectCanonicalSceneRecord"
    - path: __tests__/unit/lib/editor-scene-draft.test.ts
      provides: Coverage for draft hydration behavior
      min_lines: 25
  key_links:
    - from: app/scene-editor.tsx
      to: stores/use-app-store.ts
      via: canonical scene selector
      pattern: "selectCanonicalSceneRecord"
    - from: components/editor/SceneComposer.tsx
      to: lib/editor-scene-draft.ts
      via: editor draft hydration or guard logic
      pattern: "editor-scene-draft"
---

<objective>
Stabilize scene opening and editor draft hydration so an existing scene can be loaded, edited and preserved in memory without destructive reset behavior.

Purpose: Remove the current mount-time timeline wipe and make scene-editor routing canonical-first.
Output: A pure draft hydration helper, route-level canonical loading, and guarded editor initialization.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-editor-load-save-stability/02-CONTEXT.md
@.planning/phases/01-canonical-scene-model/01-01-SUMMARY.md
@stores/use-app-store.ts
@stores/use-editor-store.ts
@app/scene-editor.tsx
@components/editor/SceneComposer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pure editor draft hydration helpers</name>
  <files>lib/editor-scene-draft.ts, __tests__/unit/lib/editor-scene-draft.test.ts</files>
  <read_first>lib/canonical-scene.ts, stores/use-app-store.ts, stores/use-editor-store.ts, lib/engine/types.ts</read_first>
  <action>Add `lib/editor-scene-draft.ts` with pure helpers that derive editor draft state from a canonical `SceneRecord`, for example scene name, timeline and any safe default editor metadata needed by `use-editor-store`. Keep it one-way and avoid UI or hook dependencies. Write `__tests__/unit/lib/editor-scene-draft.test.ts` first and verify it fails before implementation. Cover at least: hydrating from an existing canonical record, preserving timeline, and deriving a stable fallback name for empty or partial scenes.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/editor-scene-draft.test.ts` and ensure the test fails before implementation, then passes after the helper is added.</verify>
  <acceptance_criteria>
    - `lib/editor-scene-draft.ts` exists
    - `__tests__/unit/lib/editor-scene-draft.test.ts` exists
    - Draft hydration reads canonical timeline without mutating the source record
    - The helper provides a stable scene name fallback that is not based on `StoryScene.text`
  </acceptance_criteria>
  <done>The phase has a test-backed, pure way to hydrate editor draft state from canonical scene data.</done>
</task>

<task type="auto">
  <name>Task 2: Make the scene editor route canonical-first and self-sufficient</name>
  <files>app/scene-editor.tsx, stores/use-app-store.ts, lib/editor-scene-draft.ts</files>
  <read_first>app/scene-editor.tsx, stores/use-app-store.ts, lib/editor-scene-draft.ts, components/editor/SceneManager.tsx, .planning/phases/02-editor-load-save-stability/02-CONTEXT.md</read_first>
  <action>Update `app/scene-editor.tsx` so it loads the current story as before but resolves the target scene through canonical selectors, not by reconstructing `initialSceneName` from `currentStory.scenes[sceneId].text`. The route should be able to open the scene even if navigation did not preload `useEditorStore`. Use the pure draft helper to prepare the initial editor input. Do not expand scope into SceneManager cleanup beyond what is necessary for route compatibility.</action>
  <verify>Run `pnpm check` if possible; otherwise run targeted tests and inspect the route for canonical-first access. Document any pre-existing global type blockers separately.</verify>
  <acceptance_criteria>
    - `app/scene-editor.tsx` references `selectCanonicalSceneRecord`
    - `app/scene-editor.tsx` no longer derives the editor name from `currentStory.scenes[sceneId].text`
    - The route can render `SceneComposer` with canonical scene input even when `useEditorStore` starts empty
  </acceptance_criteria>
  <done>Scene editor routing becomes self-sufficient and canonical-first.</done>
</task>

<task type="auto">
  <name>Task 3: Guard editor initialization against destructive scene reset</name>
  <files>components/editor/SceneComposer.tsx, stores/use-editor-store.ts, lib/editor-scene-draft.ts</files>
  <read_first>components/editor/SceneComposer.tsx, stores/use-editor-store.ts, lib/editor-scene-draft.ts, __tests__/unit/lib/editor-scene-draft.test.ts</read_first>
  <action>Refactor `SceneComposer` and, if needed, `use-editor-store.ts` so initial scene hydration happens once per scene change and does not blindly call `setScene(sceneId, initialSceneName, [])`. Introduce an explicit initialization/hydration boundary or guard that differentiates between first load of a scene and ongoing edits. Preserve current editor interactions and avoid adding persistence to the editor store in this phase unless strictly necessary.</action>
  <verify>Run the targeted tests from this plan and add a focused assertion if needed for the mount guard behavior.</verify>
  <acceptance_criteria>
    - `SceneComposer` no longer initializes every scene with an empty timeline
    - Scene changes still rehydrate the editor when `sceneId` actually changes
    - Editing after load does not immediately reset because of a repeated effect run
  </acceptance_criteria>
  <done>Opening an existing scene no longer destroys its draft state on mount.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/lib/editor-scene-draft.test.ts` passes
- [ ] Existing Phase 1 focused tests still pass
- [ ] Scene editor route is canonical-first and no longer depends on `StoryScene.text` for naming
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No destructive reset of canonical scene timeline on editor mount
- Opening a persisted scene hydrates editor draft state from canonical data
</success_criteria>

<output>
After completion, create `.planning/phases/02-editor-load-save-stability/02-01-SUMMARY.md`
</output>
