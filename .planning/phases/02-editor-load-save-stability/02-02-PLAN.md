---
phase: 02-editor-load-save-stability
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - components/editor/SceneComposer.tsx
  - stores/use-app-store.ts
  - stores/use-editor-store.ts
  - __tests__/unit/stores/use-app-store-canonical.test.ts
  - __tests__/unit/lib/editor-scene-save.test.ts
autonomous: true
requirements:
  - EDIT-02
  - EDIT-03
user_setup: []
must_haves:
  truths:
    - "Saving a scene merges content into the existing canonical record instead of rebuilding metadata from scratch"
    - "Connections, flow position, isStart and createdAt survive a save cycle"
    - "A saved scene reopens with the same name and timeline that were just written"
  artifacts:
    - path: components/editor/SceneComposer.tsx
      provides: Metadata-preserving save flow using canonical helpers
      contains: "updateSceneRecordPreservingMeta"
    - path: __tests__/unit/lib/editor-scene-save.test.ts
      provides: Regression coverage for metadata-safe scene saving and reopen assumptions
      min_lines: 25
    - path: __tests__/unit/stores/use-app-store-canonical.test.ts
      provides: Extended canonical store coverage for save behavior
      min_lines: 40
  key_links:
    - from: components/editor/SceneComposer.tsx
      to: stores/use-app-store.ts
      via: metadata-preserving canonical save helper
      pattern: "updateSceneRecordPreservingMeta"
    - from: __tests__/unit/lib/editor-scene-save.test.ts
      to: lib/editor-scene-draft.ts
      via: reopen and save-cycle expectations
      pattern: "editor-scene-draft"
---

<objective>
Make editor save behavior metadata-safe and protect the full `open -> edit -> save -> reopen` cycle with focused regression tests.

Purpose: Ensure edits update scene content without erasing canonical metadata or forcing the next reopen into stale or empty state.
Output: Metadata-preserving save logic, reopen-safe behavior, and regression tests for the save cycle.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-editor-load-save-stability/02-CONTEXT.md
@.planning/phases/02-editor-load-save-stability/02-01-PLAN.md
@components/editor/SceneComposer.tsx
@stores/use-app-store.ts
@stores/use-editor-store.ts
@lib/editor-scene-draft.ts
@lib/canonical-scene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for metadata-safe save and reopen behavior</name>
  <files>__tests__/unit/lib/editor-scene-save.test.ts, __tests__/unit/stores/use-app-store-canonical.test.ts</files>
  <read_first>__tests__/unit/stores/use-app-store-canonical.test.ts, __tests__/unit/lib/story-hooks-canonical-scene.test.ts, lib/editor-scene-draft.ts, stores/use-app-store.ts</read_first>
  <action>Before changing save logic, add a new focused test file `__tests__/unit/lib/editor-scene-save.test.ts` that describes the desired behavior for `open -> edit -> save -> reopen`. At minimum, cover preserving `connections`, `flowX`, `flowY`, `createdAt` and `isStart` across a save. Extend `__tests__/unit/stores/use-app-store-canonical.test.ts` if needed so the canonical store helper is protected against regressions. Verify the new tests fail for the expected reason before implementing the fix.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/editor-scene-save.test.ts __tests__/unit/stores/use-app-store-canonical.test.ts` and confirm failure is caused by missing or incorrect save behavior, not a syntax issue.</verify>
  <acceptance_criteria>
    - `__tests__/unit/lib/editor-scene-save.test.ts` exists
    - The new tests fail before the implementation change
    - At least one assertion proves metadata would be lost with the old save strategy
  </acceptance_criteria>
  <done>The save-cycle behavior is specified in executable regression tests before code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor editor save flow to merge into canonical records</name>
  <files>components/editor/SceneComposer.tsx, stores/use-app-store.ts, lib/editor-scene-draft.ts</files>
  <read_first>components/editor/SceneComposer.tsx, stores/use-app-store.ts, lib/canonical-scene.ts, lib/editor-scene-draft.ts, .planning/phases/01-canonical-scene-model/01-01-SUMMARY.md</read_first>
  <action>Update `SceneComposer.handleSave()` so it resolves the existing canonical record and uses the metadata-preserving helper rather than rebuilding a blank record inline. Preserve canonical metadata fields already tracked in the record and only update editor-owned content such as `name`, `timeline`, and safe scene-state fields. If a new scene truly has no existing canonical record yet, create it through a controlled initialization path rather than reusing the destructive inline object shape.</action>
  <verify>Run the focused save tests and Phase 1 canonical tests after the refactor.</verify>
  <acceptance_criteria>
    - `SceneComposer` does not construct a blank `SceneRecord` with empty metadata on every save
    - Existing canonical metadata survives the save
    - Newly saved content is reflected in the canonical record used for reopen
  </acceptance_criteria>
  <done>Saving a scene updates canonical content without destroying metadata or reopen consistency.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the reopen cycle with focused regression coverage</name>
  <files>__tests__/unit/lib/editor-scene-save.test.ts, __tests__/unit/lib/editor-scene-draft.test.ts, components/editor/SceneComposer.tsx</files>
  <read_first>__tests__/unit/lib/editor-scene-save.test.ts, __tests__/unit/lib/editor-scene-draft.test.ts, components/editor/SceneComposer.tsx, app/scene-editor.tsx</read_first>
  <action>Finalize the regression tests so they demonstrate the complete intended behavior: canonical scene opens into editor draft, draft changes can be saved, and reopening the scene yields the updated name and timeline while keeping metadata intact. Reuse pure helpers where possible instead of UI-heavy integration tests. Only add as much test scaffolding as is needed to protect the critical save/reopen path.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/editor-scene-draft.test.ts __tests__/unit/lib/editor-scene-save.test.ts __tests__/unit/stores/use-app-store-canonical.test.ts` and keep output clean.</verify>
  <acceptance_criteria>
    - Reopen behavior is asserted in tests, not just save behavior in isolation
    - Updated scene name and timeline are preserved after the simulated save/reopen cycle
    - Phase 2 tests document the intended editor lifecycle clearly enough for later phases
  </acceptance_criteria>
  <done>The phase has executable regression protection for `open -> edit -> save -> reopen`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/lib/editor-scene-draft.test.ts __tests__/unit/lib/editor-scene-save.test.ts __tests__/unit/stores/use-app-store-canonical.test.ts` passes
- [ ] Phase 1 focused tests still pass
- [ ] Save flow preserves canonical metadata across reopen assumptions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Save flow no longer wipes canonical metadata
- Reopened scenes retain the updated editor content that was just saved
</success_criteria>

<output>
After completion, create `.planning/phases/02-editor-load-save-stability/02-02-SUMMARY.md`
</output>
