---
phase: 17-inline-character-authoring
plan: 03
type: execute
wave: 3
depends_on: ["17-00", "17-02"]
files_modified:
  - lib/engine/useSceneExecutor.ts
  - lib/engine/runtime-types.ts
  - components/story-reader-responsive.tsx
  - hooks/useReaderAssets.ts
  - hooks/useCharacterAnimations.ts
  - hooks/useSceneImages.ts
  - components/CharacterDisplay.tsx
  - components/reader/ReaderDisplay.tsx
  - components/editor/PreviewScreen.tsx
  - __tests__/unit/lib/reader-runtime.test.ts
requirements:
  - Runtime-character-actions
  - Real-sprite-rendering
  - Responsive-character-positions
  - Speaker-focus-rendering
  - Missing-sprite-fallback
  - Reader-character-library-access
  - Character-display-accessibility
  - Reader-display-name-resolution
autonomous: true
---

<objective>
Make generated character steps visible and correct in Preview and Reader: real sprite images, responsive positioning, no repeated entrance animation, and subtle focus on the active speaker.
</objective>

<execution_notes>
This plan should be executed as two bounded slices if it does not fit in one
session:
- 03a: executor action semantics, sprite resolver wiring, missing-sprite
  fallback, and reader display-name resolution.
- 03b: Preview/Reader visual positioning, shared animated instance pipeline,
  `CharacterDisplay` interface changes, active-speaker focus, and animation
  cache fixes.
</execution_notes>

<tasks>

<task type="auto">
  <name>Execute character actions</name>
  <files>lib/engine/useSceneExecutor.ts lib/engine/runtime-types.ts</files>
  <action>
    Update executor handling for:
    - `show`: add visible character if absent, update if present
    - `hide`: mark invisible or remove from visible list
    - `change_sprite`: update sprite only
    - `move`: update position only

    Preserve existing behavior for old character steps with no `action` by
    treating them as `show`.

    Add runtime state needed for dialogue focus, including optional
    `activeSpeakerCharacterId` and focus settings derived from dialogue steps.
  </action>
  <acceptance_criteria>
    - Old persisted scenes still render.
    - New action semantics are deterministic.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Resolve character sprites from library</name>
  <files>components/story-reader-responsive.tsx hooks/useReaderAssets.ts hooks/useSceneImages.ts components/editor/PreviewScreen.tsx</files>
  <action>
    Use `characterLibraries[storyId]` and the resolver from Plan 00 to map
    character runtime state to image URIs. `StoryReaderResponsive` must read the
    character library from `useAppStore()` or pass it down from an existing store
    selector. Do not pass raw `spriteId` directly to `resolveAssetUri`.

    If no sprite is available, render a non-crashing fallback and surface a dev
    warning where appropriate.

    Reader must render visible characters even when `resolvedCharUris` has no
    URI for that character. Do not gate `ReaderCharacters` rendering only on
    `Object.keys(resolvedCharUris).length > 0`; use visible character instances
    as the source of truth and let `CharacterDisplay` show a placeholder.

    Avoid decoding all sprite assets for inline/token rendering; only resolve
    sprites needed for currently visible runtime characters or visible popover
    preview rows.
  </action>
  <acceptance_criteria>
    - Reader and Preview display uploaded sprite images.
    - Missing sprites fail gracefully without crashing.
    - Missing sprites still show a character placeholder and editor warning.
    - Reader path receives character library data before resolving sprites.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Replace Preview placeholder</name>
  <files>components/editor/PreviewScreen.tsx components/reader/ReaderDisplay.tsx</files>
  <action>
    Remove the placeholder card that displays `characterId`. Render the same
    sprite image path used by Reader. Replace fixed pixel positions with
    responsive position mapping:
    - `far-left`: 10%
    - `left`: 25%
    - `center`: 50%
    - `right`: 75%
    - `far-right`: 90%

    Treat these values as center anchors, not left edges. Apply
    `translateX(-50%)` or an equivalent centered layout so wide sprites do not
    overflow at the edges. Account for phone and desktop aspect ratios by
    anchoring from scene width, not hardcoded pixels.

    Update `ReaderCharacters` in `ReaderDisplay.tsx` to position each character
    individually, rather than relying on the current centered row layout. The
    character layer remains absolute, but each child gets its own center anchor
    based on `position`.

    PreviewScreen must build animated character instances through the same
    `useCharacterAnimations` / `buildCharacterInstance` path as Reader before
    passing them to `CharacterDisplay`.
  </action>
  <acceptance_criteria>
    - Preview shows the actual sprite.
    - Character positions scale across phone and desktop widths.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Render speaker focus</name>
  <files>hooks/useCharacterAnimations.ts components/CharacterDisplay.tsx components/reader/ReaderDisplay.tsx components/editor/PreviewScreen.tsx</files>
  <action>
    Apply `DialogueBlockData.speakerFocus` by slightly scaling the active speaker
    and optionally dimming other visible characters. Do not replay entrance
    animation when focus changes.

    Define the CharacterDisplay interface explicitly:
    - receive `position`
    - receive `isActiveSpeaker`
    - receive `dimmed`
    - receive optional `focusScale`

    Update the `useCharacterAnimations` / `buildCharacterInstance` ->
    `ReaderDisplay` -> `CharacterDisplay` pipeline so the new props are passed
    intentionally and existing animation state is preserved.

    `buildCharacterInstance` must accept the full character position set
    (`far-left`, `left`, `center`, `right`, `far-right`) or pass position
    through separately from `instance`. Focus state should be passed as separate
    `CharacterDisplay` props (`isActiveSpeaker`, `dimmed`, `focusScale`), not
    hidden inside the `instance` object.

    Ensure animated values are created once per character via cache/useMemo and
    are not recreated on every render. Fix any existing render-loop allocation in
    `useCharacterAnimations` touched by this work.

    Use existing animation values where possible so focus transitions are smooth.
    Screen-reader output must not depend on visual focus state. Add
    `accessibilityLabel` using the character display name or sprite name.
  </action>
  <acceptance_criteria>
    - Active speaker emphasis is visible but subtle.
    - Focus reset occurs when speaker changes or dialogue ends.
    - CharacterDisplay remains accessible and does not expose internal IDs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Align interactive object speaker labels</name>
  <files>components/story-reader-responsive.tsx components/reader/ReaderDisplay.tsx</files>
  <action>
    Interactive object dialogue should use the same character ID to display-name
    resolution when its speaker references a character. If full interactive
    speaker focus is too broad, document it as deferred, but keep speaker labels
    user-facing.
  </action>
  <acceptance_criteria>
    - Interactive object dialogue does not display raw `char_*` IDs.
    - Existing interactive dialogue behavior is preserved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Resolve reader speaker display names</name>
  <files>components/story-reader-responsive.tsx lib/reader-runtime.ts</files>
  <action>
    Update `getTimelineDisplayPages`, `useReaderPages`, and/or
    `StoryReaderResponsive.extractSpeaker` so displayed dialogue labels use
    `speakerName` or `characterId -> Character.name`, never raw library IDs.
    Preserve plain narration parsing.
  </action>
  <acceptance_criteria>
    - Reader speaker label shows `Маша`, not `char_*`.
    - Existing narration with colons does not become a speaker label.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Runtime tests</name>
  <files>__tests__/unit/lib/reader-runtime.test.ts existing executor tests</files>
  <action>
    Add tests for action semantics, active speaker state, centered position
    mapping, `buildCharacterInstance`/`CharacterDisplay` prop flow, reader
    character-library resolution, display-name labels, animated value caching,
    PreviewScreen instance creation, and missing sprite graceful degradation.
  </action>
</task>

</tasks>

<rollback>
If Reader integration is broader than expected, first update Preview and pure
resolver tests, then Reader. Do not leave Preview and Reader with incompatible
sprite semantics.
</rollback>

<verification>
- targeted runtime/reader tests
- `corepack pnpm run check`
- manual Preview smoke with two speakers and one sprite change
</verification>

<success_criteria>
- Preview/Reader show real user-uploaded sprites.
- Character positioning is responsive and center-anchored so wide sprites do not
  overflow at `far-left` / `far-right`.
- Active speaker focus works without repeated show animation.
</success_criteria>
