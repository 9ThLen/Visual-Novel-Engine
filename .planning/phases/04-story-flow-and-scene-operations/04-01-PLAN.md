---
phase: 04-story-flow-and-scene-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stores/use-app-store.ts
  - components/editor/SceneManager.tsx
  - hooks/useReaderInitialization.ts
  - __tests__/unit/stores/use-app-store-scene-operations.test.ts
autonomous: true
requirements:
  - EDIT-04
  - DATA-04
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - "SceneManager opens and deletes persisted canonical scenes through store actions instead of direct setState mutations"
    - "Canonical scene CRUD keeps storiesMetadata, startSceneId and scene isStart flags synchronized"
    - "Deleting or reassigning scenes does not leave orphaned canonical connections or broken reader start-scene state"
  artifacts:
    - path: stores/use-app-store.ts
      provides: Canonical scene operation actions with delete/start cleanup
      contains: "deleteSceneRecord"
    - path: components/editor/SceneManager.tsx
      provides: SceneManager flow aligned to canonical scene operation actions
      contains: "deleteSceneRecord"
    - path: __tests__/unit/stores/use-app-store-scene-operations.test.ts
      provides: Coverage for canonical delete/start-scene cleanup behavior
      min_lines: 35
  key_links:
    - from: components/editor/SceneManager.tsx
      to: stores/use-app-store.ts
      via: canonical create/duplicate/delete scene actions
      pattern: "deleteSceneRecord"
    - from: hooks/useReaderInitialization.ts
      to: stores/use-app-store.ts
      via: start-scene metadata contract
      pattern: "startSceneId"
---

<objective>
Stabilize canonical scene CRUD and start-scene cleanup so SceneManager can operate on persisted records safely.

Purpose: Remove direct state mutations and ensure create/delete/set-start flows keep graph metadata and reader entry state consistent.
Output: Canonical scene operation actions, SceneManager refactor, and regression tests for delete/start cleanup.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-story-flow-and-scene-operations/04-CONTEXT.md
@stores/use-app-store.ts
@components/editor/SceneManager.tsx
@hooks/useReaderInitialization.ts
@lib/editor-scene-draft.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for canonical scene operation cleanup</name>
  <files>__tests__/unit/stores/use-app-store-scene-operations.test.ts</files>
  <read_first>stores/use-app-store.ts, __tests__/unit/stores/use-app-store-canonical.test.ts, hooks/useReaderInitialization.ts, lib/editor-scene-draft.ts</read_first>
  <action>Create focused tests that define the desired canonical behavior for scene create/delete/start flows. At minimum, cover: deleting a canonical scene removes inbound and outbound connections, deleting the current start scene reassigns or clears `storiesMetadata.startSceneId` safely, and canonical create/save of the first scene keeps start-scene metadata aligned. Verify these tests fail before implementation for the expected reason.</action>
  <verify>Run `pnpm test -- __tests__/unit/stores/use-app-store-scene-operations.test.ts` and confirm failure is caused by missing canonical scene operation behavior.</verify>
  <acceptance_criteria>
    - `__tests__/unit/stores/use-app-store-scene-operations.test.ts` exists
    - The new test fails before implementation
    - At least one test proves delete cleanup removes orphaned connections
  </acceptance_criteria>
  <done>The desired canonical scene CRUD cleanup behavior is captured in executable tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical scene operation actions to the store</name>
  <files>stores/use-app-store.ts, hooks/useReaderInitialization.ts</files>
  <read_first>stores/use-app-store.ts, hooks/useReaderInitialization.ts, lib/runtime-story.ts, .planning/phases/04-story-flow-and-scene-operations/04-CONTEXT.md</read_first>
  <action>Add or refine canonical store actions such as `createSceneRecord`, `duplicateSceneRecord`, `deleteSceneRecord` and any needed start-scene synchronization helpers. Ensure delete cleanup removes inbound/outbound connections, updates `sceneCount`, keeps `updatedAt` fresh, and leaves `storiesMetadata.startSceneId` in a valid state. If reader initialization still assumes metadata is always populated, add the narrow fallback needed to preserve reader entry after delete/reassign flows.</action>
  <verify>Run the new store operation tests and existing canonical persistence/runtime tests after implementation.</verify>
  <acceptance_criteria>
    - `stores/use-app-store.ts` exposes a canonical delete action and start-scene synchronization path
    - Deleting a scene cannot leave orphaned canonical connections
    - Start-scene metadata stays aligned with canonical scene flags after create/delete/set-start flows
  </acceptance_criteria>
  <done>The store owns canonical scene CRUD invariants instead of relying on ad hoc component mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor SceneManager to use canonical scene operation actions</name>
  <files>components/editor/SceneManager.tsx, stores/use-app-store.ts</files>
  <read_first>components/editor/SceneManager.tsx, app/scene-editor.tsx, stores/use-app-store.ts, lib/editor-scene-draft.ts</read_first>
  <action>Refactor `SceneManager.tsx` so create/duplicate/delete flows route through the new canonical store actions and no longer use direct `useAppStore.setState(...)`. Preserve existing UX shape, but remove unsafe direct state mutation and normalize new scene creation around a shared canonical record shape rather than `sceneState: {} as any` style placeholders. Keep scene opening pointed at the persisted canonical record already used by `scene-editor.tsx`.</action>
  <verify>Run the new store tests and the focused Phase 2/3 regression suite after implementation.</verify>
  <acceptance_criteria>
    - `SceneManager.tsx` does not implement canonical delete by direct `useAppStore.setState(...)`
    - New and duplicated scenes are created through a canonical store path
    - SceneManager edit/open flow remains routed to persisted canonical scenes
  </acceptance_criteria>
  <done>SceneManager becomes a thin UI over canonical scene operation actions rather than a second source of mutation logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/stores/use-app-store-scene-operations.test.ts` passes
- [ ] Existing Phase 1-3 focused tests still pass
- [ ] SceneManager no longer uses direct state mutation for canonical scene delete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Canonical scene CRUD owns delete/start cleanup invariants
- SceneManager opens persisted canonical scenes through the aligned store path
</success_criteria>

<output>
After completion, create `.planning/phases/04-story-flow-and-scene-operations/04-01-SUMMARY.md`
</output>
