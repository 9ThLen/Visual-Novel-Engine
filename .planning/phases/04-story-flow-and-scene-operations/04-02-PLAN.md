---
phase: 04-story-flow-and-scene-operations
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - lib/story-flow-graph.ts
  - stores/use-app-store.ts
  - components/editor/StoryFlowScreen.tsx
  - components/editor/SceneSelector.tsx
  - components/editor/SceneComposer.tsx
  - __tests__/unit/lib/story-flow-graph.test.ts
  - __tests__/unit/stores/use-app-store-scene-operations.test.ts
autonomous: true
requirements:
  - DATA-04
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - "StoryFlow derives nodes and connections from canonical scene records and stays synchronized after reload or navigation"
    - "Scene positions, start-scene state and connection CRUD persist through canonical store actions without stale local graph state"
    - "Connection removal and updates are port-aware, so graph edits do not leave broken or ambiguous links"
  artifacts:
    - path: lib/story-flow-graph.ts
      provides: Pure helpers for deriving flow nodes/edges from canonical scene records
      min_lines: 35
    - path: components/editor/StoryFlowScreen.tsx
      provides: StoryFlow UI synchronized to canonical graph data
      contains: "story-flow-graph"
    - path: __tests__/unit/lib/story-flow-graph.test.ts
      provides: Coverage for canonical graph derivation and position/start sync behavior
      min_lines: 35
  key_links:
    - from: components/editor/StoryFlowScreen.tsx
      to: lib/story-flow-graph.ts
      via: derived canonical nodes and edges
      pattern: "story-flow-graph"
    - from: components/editor/SceneComposer.tsx
      to: stores/use-app-store.ts
      via: canonical connection action path
      pattern: "updateSceneConnection"
---

<objective>
Stabilize StoryFlow synchronization, position persistence and connection CRUD around canonical scene records.

Purpose: Ensure graph editing reflects the same scene data the store persists, instead of drifting through stale local node state or partial connection semantics.
Output: Derived graph helper layer, StoryFlow store synchronization, and port-aware connection editing/cleanup behavior.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-story-flow-and-scene-operations/04-CONTEXT.md
@.planning/phases/04-story-flow-and-scene-operations/04-01-PLAN.md
@components/editor/StoryFlowScreen.tsx
@components/editor/SceneSelector.tsx
@components/editor/SceneComposer.tsx
@stores/use-app-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for canonical story-flow derivation and connection semantics</name>
  <files>__tests__/unit/lib/story-flow-graph.test.ts, __tests__/unit/stores/use-app-store-scene-operations.test.ts</files>
  <read_first>components/editor/StoryFlowScreen.tsx, stores/use-app-store.ts, __tests__/unit/stores/use-app-store-scene-operations.test.ts, lib/runtime-story.ts</read_first>
  <action>Create focused tests that define the desired canonical graph behavior for StoryFlow. At minimum, cover: deriving nodes/edges from persisted scene records, preserving valid `flowX/flowY` values including `0`, and removing or updating only the targeted connection when multiple links share the same target but different `outputPort` values. Verify these tests fail before implementation for the expected reason.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/story-flow-graph.test.ts __tests__/unit/stores/use-app-store-scene-operations.test.ts` and confirm failure is caused by missing graph/connection behavior, not setup noise.</verify>
  <acceptance_criteria>
    - `__tests__/unit/lib/story-flow-graph.test.ts` exists
    - The new tests fail before implementation
    - At least one test proves connection removal is port-aware
  </acceptance_criteria>
  <done>The desired StoryFlow synchronization and connection semantics are specified in executable tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce a pure canonical graph derivation layer and harden connection actions</name>
  <files>lib/story-flow-graph.ts, stores/use-app-store.ts</files>
  <read_first>components/editor/StoryFlowScreen.tsx, stores/use-app-store.ts, lib/canonical-scene.ts, .planning/phases/04-story-flow-and-scene-operations/04-CONTEXT.md</read_first>
  <action>Add a pure helper module such as `lib/story-flow-graph.ts` that derives StoryFlow-ready nodes/edges from canonical scene records and metadata. In the store, refine connection actions so add/remove/update semantics are explicit, port-aware and safe for repeated targets. Preserve the existing canonical model; this task is about derivation and invariants, not about inventing a new graph schema.</action>
  <verify>Run the new graph tests and the existing canonical scene operation tests after implementation.</verify>
  <acceptance_criteria>
    - `lib/story-flow-graph.ts` exists
    - Connection CRUD in the store can distinguish links by `targetSceneId` and `outputPort`
    - Graph derivation preserves persisted positions and start-scene indicators without stale fallback defaults
  </acceptance_criteria>
  <done>The codebase has one pure canonical path for turning scene records into StoryFlow graph data.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor StoryFlow and narrow selector integration onto canonical graph data</name>
  <files>components/editor/StoryFlowScreen.tsx, components/editor/SceneSelector.tsx, components/editor/SceneComposer.tsx</files>
  <read_first>components/editor/StoryFlowScreen.tsx, components/editor/SceneSelector.tsx, components/editor/SceneComposer.tsx, lib/story-flow-graph.ts, stores/use-app-store.ts</read_first>
  <action>Refactor `StoryFlowScreen.tsx` to derive or synchronize its graph state from canonical store data rather than holding a stale one-time `initialNodes` snapshot. Fix persisted position handling (`0` coordinates must remain valid), wire StoryFlow connection creation/removal through the hardened store actions, and only touch `SceneSelector` / `SceneComposer` as narrowly as needed to avoid mixed template-vs-connection semantics blocking the canonical connection path. Keep this focused on reliability, not on a visual redesign.</action>
  <verify>Run the graph tests, the scene operation tests and the focused Phase 2-3 regression suite after implementation.</verify>
  <acceptance_criteria>
    - `StoryFlowScreen.tsx` no longer relies on an unsynchronized initial local graph snapshot as its only source of truth
    - Dragged positions persist and reload correctly, including `0` values
    - Connection create/remove flows use canonical store actions and remain consistent with start-scene state
  </acceptance_criteria>
  <done>StoryFlow becomes a synchronized view/editor over canonical graph data instead of a parallel local graph model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/lib/story-flow-graph.test.ts __tests__/unit/stores/use-app-store-scene-operations.test.ts` passes
- [ ] Existing Phase 1-3 focused tests still pass
- [ ] StoryFlow reflects persisted canonical positions, start-scene state and connection edits after reload/navigation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- StoryFlow reads and edits canonical graph data consistently
- Connection CRUD and position persistence no longer drift from the store state
</success_criteria>

<output>
After completion, create `.planning/phases/04-story-flow-and-scene-operations/04-02-SUMMARY.md`
</output>
