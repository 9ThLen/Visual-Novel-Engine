---
phase: 16-plate-editor-migration
plan: 01
type: execute
wave: 1
depends_on: ["16-00"]
files_modified:
  - lib/document-editor/document-scene.ts
  - lib/document-scene-persistence.ts
  - lib/vn-plate-editor/scene-normalizer.ts
  - components/editor/plate/serializers/scene-to-plate.ts
  - components/editor/plate/serializers/plate-to-scene.ts
  - components/editor/plate/types.ts
  - components/editor/plate/commands/step-validation.ts
  - __tests__/unit/editor/plate-scene-roundtrip.test.ts
requirements:
  - Roundtrip-contract
  - Technical-block-data-preservation
  - SceneRecord-TimelineStep-canonical
autonomous: true
---

<objective>
Build the canonical serializer contract before changing UI. The first green test
must prove a real technical background payload survives roundtrip unchanged.
Start by testing the existing `DocumentScene` / `vn-plate-editor` bridge; only
introduce new `components/editor/plate/serializers/*` wrappers if they clarify
the boundary or are needed by the real Plate document shape from Wave 0.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>First failing test: current bridge preserves background technical data</name>
  <files>__tests__/unit/editor/plate-scene-roundtrip.test.ts</files>
  <behavior>
    SceneRecord with:
    `background(assetId: "bg_forest", transition: "dissolve", duration: 1000)`
    converts through the current baseline:
    `SceneRecord -> DocumentScene/Plate node shape -> SceneRecord` and preserves:
    `assetId === "bg_forest"`, `transition === "dissolve"`, `duration === 1000`.
  </behavior>
  <verify>
    Run targeted test and confirm it fails before implementation.
  </verify>
</task>

<task type="auto">
  <name>Inventory and harden existing bridge types</name>
  <files>lib/document-editor/document-scene.ts lib/vn-plate-editor/scene-normalizer.ts components/editor/plate/types.ts</files>
  <action>
    First map the existing `DocumentScene`, `DocumentBlock`, and
    `DocumentTechnicalBlock` types. If they are sufficient, wrap/re-export them
    from `components/editor/plate/types.ts` as compatibility types. If the real
    Plate API needs a different node shape, define the new shape there.
    Technical nodes must carry a full valid `TimelineStep`, not a
    nullable/default-only placeholder.

    Decision branch:
    - If Wave 0 confirms `DocumentScene` / `DocumentBlock` can be the Plate node
      payload, re-export them from `components/editor/plate/types.ts` as
      compatibility types and avoid a second model.
    - If Wave 0 confirms Plate requires a different node shape, create explicit
      Plate node types in `components/editor/plate/types.ts`.
    - If both shapes are needed, create a narrow mapping layer and keep
      `SceneRecord + TimelineStep` as the source of truth.
  </action>
</task>

<task type="auto">
  <name>Promote scene-to-plate serializer from existing conversion</name>
  <files>lib/document-editor/document-scene.ts components/editor/plate/serializers/scene-to-plate.ts</files>
  <action>
    Use `sceneRecordToDocumentScene` as the baseline. Either harden it in place
    or call it from the new serializer wrapper. Preserve `step.id`, `blockType`,
    `enabled`, `collapsed`, `conditions`, and full `data`.
  </action>
</task>

<task type="auto">
  <name>Promote plate-to-scene serializer from existing save bridge</name>
  <files>lib/document-scene-persistence.ts lib/document-editor/document-scene.ts lib/vn-plate-editor/scene-normalizer.ts components/editor/plate/serializers/plate-to-scene.ts</files>
  <action>
    Use `saveDocumentSceneToRecord`, `documentSceneToTimeline`, and
    `normalizePlateDocumentScene` as baseline. Fix the data-loss path where a
    technical block with a valid `TimelineStep` is replaced by a default step.
    Preserve metadata from the source record; overwrite only `name`, `timeline`,
    `connections`, and `updatedAt`.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Expand roundtrip coverage</name>
  <behavior>
    Add tests for:
    - character `characterId`, `spriteId`, position, transition
    - dialogue with multiple entries
    - choice options with `targetSceneId`
    - music/sound/effect/camera/variable/interactive_object payloads
    - `conditions`
  </behavior>
</task>

</tasks>

<rollback>
If existing bridge serializers cannot preserve all tested payloads, do not move
to UI replacement. Keep the current `DocumentScene` format active, document the
missing cases in the failing tests, and fix serializer coverage first.
</rollback>

<verification>
- `corepack pnpm run test __tests__/unit/editor/plate-scene-roundtrip.test.ts`
- `corepack pnpm run check`
</verification>

<success_criteria>
- No serializer silently creates default steps when a valid step exists.
- Existing bridge conversions are covered before any replacement serializer is trusted.
- All current `BlockType` payloads roundtrip.
- `SceneRecord + TimelineStep` remains the only persisted contract.
</success_criteria>
