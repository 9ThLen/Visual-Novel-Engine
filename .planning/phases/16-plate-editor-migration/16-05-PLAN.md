---
phase: 16-plate-editor-migration
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - app/document-editor.tsx
  - components/document-editor/
  - components/vn-plate-editor/
  - lib/document-editor/
  - lib/vn-plate-editor/
  - README.md
  - AGENTS.md
  - docs/SCENE-MODEL-CONTRACT.md
requirements:
  - Route-switch
  - Deprecated-bridge
  - Documentation-update
  - Final-verification
autonomous: true
---

<objective>
Switch the active route to the new Plate editor, mark old bridge code as
deprecated/reference, and update project rules.
</objective>

<tasks>

<task type="auto">
  <name>Route /document-editor to PlateSceneEditor</name>
  <files>app/document-editor.tsx</files>
  <action>
    Replace `DocumentSceneEditor` usage with `PlateSceneEditor` only after Wave 4
    proves the new component wraps or supersedes the existing bridge. Keep scene
    selection, character library save, preview/back behavior, and canonical
    `saveSceneRecord`.
  </action>
</task>

<task type="auto">
  <name>Deprecate only superseded bridge modules</name>
  <files>components/document-editor/ lib/document-editor/ components/vn-plate-editor/ lib/vn-plate-editor/</files>
  <action>
    Add concise `@deprecated` headers only to files that are no longer used by
    the active `PlateSceneEditor`. If header/sidebar/inspector or conversion
    helpers remain active, move them under `components/editor/plate` /
    `lib/editor/plate` or leave them without deprecation and document why.
  </action>
</task>

<task type="auto">
  <name>Update docs and rules</name>
  <files>README.md AGENTS.md docs/SCENE-MODEL-CONTRACT.md</files>
  <action>
    Replace Lego-only rule with:
    `Use Plate as the only scene editing system. Legacy Lego components must not
    be imported by active editor screens.`
    Update architecture sections to name `components/editor/plate`.
  </action>
</task>

<task type="auto">
  <name>Final migration verification</name>
  <action>
    Run typecheck, targeted tests, serializer tests, boundary script, and manual
    editor smoke on web/native where available.
  </action>
</task>

</tasks>

<rollback>
If `/document-editor` regresses after route switch, revert the route to the
previous `DocumentSceneEditor` baseline on the migration branch. Keep serializer
tests and non-route refactors. Do not update README/AGENTS to Plate-only until
the active route is stable.
</rollback>

<verification>
- `corepack pnpm run check`
- targeted Plate roundtrip tests
- boundary script
- `rg -n "Use Lego|SceneComposer|useEditorStore" README.md AGENTS.md app components/editor`
- manual smoke: edit scene, save, preview, reload, verify data persists
</verification>

<success_criteria>
- `/document-editor` uses `PlateSceneEditor`.
- Old bridge code is explicitly deprecated.
- README/AGENTS enforce Plate-only active editing.
- No active editor screen imports legacy Lego UI.
</success_criteria>
