---
phase: 16-plate-editor-migration
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - components/editor-legacy/
  - components/editor/index.ts
  - hooks/useSceneComposerShortcuts.ts
  - components/editor/modals/AssetPicker.tsx
  - components/editor/modals/CharacterCreator.tsx
  - components/editor/modals/SaveSceneDialog.tsx
  - components/editor/SceneSelector.tsx
  - components/editor/PlayMode.tsx
  - lib/editor-scene-draft.ts
  - stores/use-editor-store.ts
  - eslint.config.mjs
  - tools/check-editor-boundaries.ps1
  - package.json
requirements:
  - Active-legacy-manuscript-split
  - Import-boundary
  - Manuscript-editor-preserved
autonomous: true
---

<objective>
Physically separate active Plate/manuscript code from legacy Lego UI, then add
an automated boundary so active routes cannot import legacy code.
</objective>

<tasks>

<task type="auto">
  <name>Move Lego UI to editor-legacy</name>
  <action>
    Move only confirmed Lego editor UI into `components/editor-legacy/`:
    - `components/editor/SceneComposer.tsx`
    - `components/editor/SceneComposerDesktop.tsx`
    - `components/editor/SceneComposerPhone.tsx`
    - `components/editor/TimelinePanel.tsx`
    - `components/editor/BlockLibraryPanel.tsx`
    - `components/editor/PropertiesPanel.tsx`
    - `components/editor/properties/*`
    - `components/editor/MiniPreview.tsx`
    - `hooks/useSceneComposerShortcuts.ts` (exists; move or rename under legacy)

    Preserve imports inside the legacy subtree.
  </action>
</task>

<task type="auto">
  <name>Classify shared/active editor files before moving</name>
  <action>
    Do not blindly move these files. Classify them and update imports:
    - `components/editor/modals/AssetPicker.tsx`: active/shared today because
      `DocumentTechnicalPropertiesPanel` imports it. Keep active or move to a
      shared/plate path before moving legacy properties.
    - `components/editor/modals/CharacterCreator.tsx`: move to legacy only if
      no active Plate/document surface imports it.
    - `components/editor/modals/SaveSceneDialog.tsx`: move to legacy only if
      no active save/load surface imports it.
    - `components/editor/SceneSelector.tsx`: active today because
      `SceneManager` imports it. Keep active/shared unless SceneManager is
      changed to a replacement.
    - `components/editor/PlayMode.tsx`: active because `app/play.tsx` imports
      it. Keep active or move to a non-editor runtime path with route updated.
    - `lib/editor-scene-draft.ts`: still used by `SceneManager` to create
      records; do not move until replacement factory exists.
  </action>
</task>

<task type="auto">
  <name>Keep manuscript active</name>
  <action>
    Keep `StoryManuscriptScreen` and `components/editor/manuscript/*` in active
    `components/editor/`, or move them to `components/editor/manuscript/` with
    route imports updated. Do not put manuscript under legacy unless product
    explicitly retires it.
  </action>
</task>

<task type="auto">
  <name>Explicit active keep-list</name>
  <action>
    These files remain active unless a later task moves them with updated imports:
    - `components/editor/PreviewScreen.tsx`
    - `components/editor/SceneManager.tsx`
    - `components/editor/PlayMode.tsx`
    - `components/editor/SceneSelector.tsx`
    - `components/editor/StoryManuscriptScreen.tsx`
    - `components/editor/manuscript/*`
    - `components/editor/modals/AssetPicker.tsx` while document/Plate technical
      editing still imports it
  </action>
</task>

<task type="auto">
  <name>Restrict active exports</name>
  <files>components/editor/index.ts</files>
  <action>
    Export active editor surfaces only: Plate editor, manuscript, preview/play
    if still housed here. Do not export Lego components from active index.
  </action>
</task>

<task type="auto">
  <name>Add import boundary</name>
  <files>eslint.config.mjs tools/check-editor-boundaries.ps1 package.json</files>
  <action>
    Add either ESLint `no-restricted-imports` or a deterministic script that
    fails if `app/**` or active `components/editor/**` import:
    `components/editor-legacy`, `SceneComposer`, `TimelinePanel`,
    `BlockLibraryPanel`, `PropertiesPanel`, or `stores/use-editor-store`.
  </action>
</task>

</tasks>

<rollback>
If typecheck fails after moving files, revert the legacy move commit and redo it
in smaller groups: composer core first, properties second, shared modals last.
Do not move `SceneSelector`, `PlayMode`, `AssetPicker`, or
`lib/editor-scene-draft.ts` unless all active imports are updated.
</rollback>

<verification>
- `corepack pnpm run check`
- boundary script exits 0
- `rg -n "components/editor-legacy|SceneComposer|TimelinePanel|BlockLibraryPanel|PropertiesPanel|useEditorStore" app components/editor`
</verification>

<success_criteria>
- Legacy code can exist, but active routes cannot import it.
- Manuscript editor still compiles.
- Boundary is automated.
</success_criteria>
