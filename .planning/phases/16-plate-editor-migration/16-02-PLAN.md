---
phase: 16-plate-editor-migration
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - components/editor/PreviewScreen.tsx
  - components/editor/SceneManager.tsx
  - __tests__/unit/editor/preview-source.test.ts
requirements:
  - Remove-useEditorStore-active-path
  - Preview-reads-persisted-SceneRecord
  - SceneManager-navigation-only
autonomous: true
---

<objective>
Cut the active screens away from Lego draft state while keeping current behavior.
`app/document-editor.tsx` already loads `SceneRecord` from `useAppStore`; this
plan must preserve that invariant, not reimplement it.
</objective>

<tasks>

<task type="auto">
  <name>PreviewScreen reads persisted timeline only</name>
  <files>components/editor/PreviewScreen.tsx</files>
  <action>
    Remove `useEditorStore` import and selectors. Timeline source becomes:
    `sceneRecordsByStory[storyId]?.[sceneId]?.timeline ?? []`.
  </action>
  <acceptance_criteria>
    `PreviewScreen.tsx` has no `useEditorStore` string.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>SceneManager navigates without draft hydration</name>
  <files>components/editor/SceneManager.tsx</files>
  <action>
    Remove `useEditorStore.getState().setScene(...)` from edit/import flows.
    Keep `router.push({ pathname: '/document-editor', params: { storyId, sceneId } })`.
  </action>
</task>

<task type="auto">
  <name>Ensure document route owns initial conversion</name>
  <files>app/document-editor.tsx</files>
  <action>
    Confirm the route still loads `SceneRecord` from `useAppStore` and prepares
    editor input itself. No code change is expected unless a stale
    `useEditorStore` dependency is found. Add a comment only if it clarifies the
    invariant without noise.
  </action>
  <acceptance_criteria>
    `app/document-editor.tsx` has no `useEditorStore` import before and after
    this plan.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Add source-of-truth regression tests</name>
  <files>__tests__/unit/editor/preview-source.test.ts</files>
  <behavior>
    Test that preview uses persisted scene timeline even if legacy editor store
    contains a dirty timeline for the same scene.
  </behavior>
</task>

</tasks>

<rollback>
If preview or scene navigation regresses after removing `useEditorStore`, revert
only the Wave 2 changes on the migration branch. Do not continue to legacy moves
until the persisted `SceneRecord` source path is fixed. A temporary restoration
of `useEditorStore` is allowed only with a TODO and a failing regression test
that captures the blocker.
</rollback>

<verification>
- `rg -n "useEditorStore" components/editor/PreviewScreen.tsx components/editor/SceneManager.tsx app/document-editor.tsx`
- targeted tests
- `corepack pnpm run check`
</verification>

<success_criteria>
- Active preview/navigation no longer depends on Lego draft state.
- `/document-editor` remains reachable from SceneManager.
</success_criteria>
