---
phase: 15-deep-audit-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useTypewriter.ts
  - __tests__/unit/hooks/use-typewriter.test.ts
  - lib/_core/theme.ts
  - components/document-editor/DocumentSceneSidebar.tsx
  - components/document-editor/DocumentBlockDialogue.tsx
  - components/document-editor/DocumentCommandMenu.tsx
  - components/document-editor/DocumentChip.tsx
  - components/editor/manuscript/StoryManuscriptBlock.tsx
  - components/editor/manuscript/StoryManuscriptSidebar.tsx
  - components/editor/PreviewScreen.tsx
  - components/editor/TimelinePanel.tsx
  - lib/story-reader-platform.ts
  - components/ui/icon-symbol.tsx
  - components/SplashScreenEditor.tsx
  - components/InteractiveObjectsEditor.tsx
  - components/editor/SceneSelector.tsx
  - components/editor/SceneComposerPhone.tsx
  - lib/translations.ts
  - app/preview.tsx
  - components/editor/MiniPreview.tsx
  - components/story-reader-responsive.tsx
  - components/reader/ReaderControls.tsx
  - components/reader/ReaderChoices.tsx
  - components/editor/properties/panel-chrome.tsx
  - components/editor/properties/asset-field.tsx
  - components/document-editor/DocumentBlockChoice.tsx
  - components/document-editor/DocumentBlockDialogue.tsx
  - components/document-editor/DocumentPage.tsx
  - lib/editor/story-manuscript-save.ts
  - __tests__/unit/lib/story-manuscript-save.test.ts
autonomous: true
requirements:
  - CR-1
  - CR-2
  - CR-3
  - CR-4
  - CR-5
  - CR-6
  - CR-7
must_haves:
  truths:
    - "User can change text speed mid-line and the next character types at the new speed"
    - "No component file uses `${colors.<name>}NN` hex+alpha string concatenation"
    - "No native reader black/white color fallback is hardcoded; consumers must pass colors"
    - "No component file uses Unicode glyphs (▲▼←→✓) as primary command/nav controls"
    - "All 21 documented hardcoded English a11y/UI strings are replaced with `t()` calls"
    - "`app/preview.tsx` shows a spinner + t('common.loading') while loading, and t('document.invalidRoute') + a back button when route is invalid"
    - "`lib/editor/story-manuscript-save.ts` dedups `sourceStepId` in production builds (not only in __DEV__)"
  artifacts:
    - path: "hooks/useTypewriter.ts"
      provides: "Typewriter hook reading textSpeedRef.current on every interval tick"
      contains: "textSpeedRef"
    - path: "__tests__/unit/hooks/use-typewriter.test.ts"
      provides: "Unit test verifying mid-typing speed change"
      contains: "describe.*useTypewriter"
    - path: "components/document-editor/DocumentSceneSidebar.tsx"
      provides: "Hex+alpha migration; imports withAlpha from theme"
      contains: "import { withAlpha }"
    - path: "components/editor/TimelinePanel.tsx"
      provides: "Tokenized shadowColor"
      contains: "colors.foreground"
    - path: "lib/story-reader-platform.ts"
      provides: "null fallbacks for platform-layer colors"
      contains: ": string | null = null"
    - path: "components/ui/icon-symbol.tsx"
      provides: "IconSymbol MAPPING with chevron.up/down, arrow.left/right, xmark, checkmark, plus.circle, minus, square.and.arrow.up"
      contains: "'chevron.up'"
    - path: "lib/translations.ts"
      provides: "20+ new translation keys for en and uk maps (pl is added in Wave 2 via WR-3)"
      contains: "'common.loading'"
    - path: "app/preview.tsx"
      provides: "Loading state with ActivityIndicator; invalid-route state with back button"
      contains: "ActivityIndicator"
    - path: "lib/editor/story-manuscript-save.ts"
      provides: "Dedup logic outside __DEV__ guard"
      contains: "seenSourceStepIds.has(sourceStepId)"
  key_links:
    - from: "hooks/useTypewriter.ts"
      to: "charDelayMs(textSpeedRef.current)"
      via: "interval tick reads live ref"
      pattern: "charDelayMs\\(textSpeedRef\\.current\\)"
    - from: "components/ui/icon-symbol.tsx"
      to: "components/SplashScreenEditor.tsx, InteractiveObjectsEditor.tsx, PreviewScreen.tsx, SceneSelector.tsx, SceneComposerPhone.tsx"
      via: "IconSymbol name prop"
      pattern: "IconSymbol name=\"(chevron\\.up|chevron\\.down|arrow\\.left|arrow\\.right|checkmark|xmark)\""
    - from: "lib/translations.ts"
      to: "10 component files using t()"
      via: "t('common.loading'), t('document.invalidRoute'), t('splash.configured'), etc."
      pattern: "t\\('(common\\.loading|document\\.invalidRoute|splash\\.configured|reader\\.continueHint|reader\\.hints\\.back|reader\\.hints\\.menu|reader\\.hints\\.save|editor\\.properties\\.closeHint|editor\\.properties\\.duplicateHint|editor\\.properties\\.deleteHint|editor\\.properties\\.assetFieldHint|editor\\.selectX|document\\.choice\\.addOption|document\\.choice\\.removeOption|document\\.choice\\.editOption|document\\.choice\\.moveOption|document\\.dialogue\\.characterHint|document\\.dialogue\\.lineHint|document\\.a11y\\.sceneTitle|document\\.a11y\\.narration|document\\.a11y\\.slashCommandInput)'"
---

<objective>
Resolve all 7 CRITICAL findings from the 2026-06-04 deep audit so the codebase is ready for a Beta release.

Purpose: Close user-visible bugs (useTypewriter textSpeed, story-manuscript-save production dedup), P0 theme/visual violations (withAlpha migration, hardcoded #000/#fff, Unicode glyphs), i18n P0 (21 hardcoded a11y strings), and UX dead-ends (app/preview.tsx error state).

Output: 7 atomic commits corresponding to CR-1..CR-7; one new unit test file for useTypewriter; one new translation key block in lib/translations.ts; modified MAPPING in icon-symbol.tsx; no remaining hex+alpha string concat in the targeted components.
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deep-audit-remediation/15-CONTEXT.md
@.planning/phases/14-audit-hardening/14-PLAN.md

# Source audits
@AUDIT-FULL-2026-06-04.md
@PLAN-FIXES-2026-06-04.md
@DEEP-CODE-REVIEW-2026-06-04.md
@DEEP-UI-REVIEW-2026-06-04.md

# Project rules and design contracts
@AGENTS.md
@UI-SPEC.md
@lib/_core/theme.ts
@lib/design-tokens.ts
@lib/translations.ts
@lib/i18n.ts
@components/ui/icon-symbol.tsx
@hooks/useTypewriter.ts
@lib/editor/story-manuscript-save.ts
@lib/story-reader-platform.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CR-1): useTypewriter reads textSpeed live on every tick</name>
  <files>hooks/useTypewriter.ts, __tests__/unit/hooks/use-typewriter.test.ts</files>
  <read_first>
    - hooks/useTypewriter.ts (target file)
    - lib/user-settings.ts:25 (charDelayMs source of truth)
    - __tests__/unit/hooks/use-reader-auto-advance.test.ts (test pattern: vi.useFakeTimers, renderHook from @testing-library/react, globals: true vitest config)
    - vitest.config.ts (resolve aliases)
    - vitest.setup.ts (mockExemptFiles list — add use-typewriter if needed)
  </read_first>
  <behavior>
    - Test 1: after startTypewriter('hello world'), advancing textSpeed from 0.0 to 1.0 (slowest→fastest, delays 60ms→12ms) before the interval completes changes the next tick's `delay` to ~12ms (verify via vi.getRealSystemTime / fake timer advance)
    - Test 2: clearing the interval on unmount prevents the late callback from firing
    - Test 3: completeTypewriter() sets displayedText to the full target and clears the interval
  </behavior>
  <action>
    In `hooks/useTypewriter.ts`:
    1. Add `const textSpeedRef = useRef(textSpeed);` near the top of the hook (after the existing `typewriterRef` / `currentTarget` declarations).
    2. Add `useEffect(() => { textSpeedRef.current = textSpeed; }, [textSpeed]);` immediately after the refs (this is a top-level effect, NOT inside `startTypewriter`).
    3. Inside the `setInterval` callback inside `startTypewriter` (the existing tick body), compute `const delay = charDelayMs(textSpeedRef.current);` and pass that `delay` to `setInterval(..., delay)` for the initial schedule, AND re-read the same `delay` on each tick if you want a per-tick cadence (the minimal change is to use the ref in the initial setInterval — the next interval's start will pick up the latest textSpeed via textSpeedRef). Concretely: change `setInterval(() => { ... }, delay)` so that `delay = charDelayMs(textSpeedRef.current)` is recomputed ONCE before setInterval is created (using the ref ensures we never read a stale closure value), and add a comment `// Read textSpeed live via ref; updated above on every textSpeed change`.
    4. Remove `textSpeed` from the dependency array of `startTypewriter` (currently `[textSpeed]` on line 31). Change to `[]`. The ref handles live reads. Add a one-line JSDoc above the `useEffect` that updates the ref: `// Mirror textSpeed into ref so the running interval reads the latest value without restarting`.

    In `__tests__/unit/hooks/use-typewriter.test.ts` (CREATE new file):
    1. Use `globals: true` — no `import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest'`. Use `renderHook, act` from `@testing-library/react` (already a dep).
    2. Mock pattern: render the hook via `renderHook(() => useTypewriter(initialSpeed))` then `result.current.startTypewriter('hello world')`.
    3. Use `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach` — match the pattern in use-reader-auto-advance.test.ts.
    4. Test "mid-typing speed change": after starting typewriter at speed 0, advance one tick, then change speed to 1, then advance ticks and verify interval period matches `charDelayMs(1)` (≈12ms), NOT `charDelayMs(0)` (≈60ms).
  </action>
  <verify>
    <automated>pnpm test __tests__/unit/hooks/use-typewriter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test __tests__/unit/hooks/use-typewriter.test.ts` exits 0; at least 3 test cases pass
    - `hooks/useTypewriter.ts:1-50` contains the literal tokens `textSpeedRef`, `textSpeedRef.current`, and the `useEffect(() => { textSpeedRef.current = textSpeed; }, [textSpeed])` block
    - `startTypewriter` useCallback dependency array is `[]` (no `textSpeed` reference)
    - `grep -n "charDelayMs(textSpeed)" hooks/useTypewriter.ts` returns 0 matches (every call must use the ref)
  </acceptance_criteria>
  <done>
    useTypewriter reads textSpeed live via textSpeedRef. Mid-typing speed change takes effect on the next tick. Unit test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2 (CR-2): Replace 10 hex+alpha concat sites with withAlpha()</name>
  <files>
    - components/document-editor/DocumentSceneSidebar.tsx
    - components/document-editor/DocumentBlockDialogue.tsx
    - components/document-editor/DocumentCommandMenu.tsx
    - components/document-editor/DocumentChip.tsx
    - components/editor/manuscript/StoryManuscriptBlock.tsx
    - components/editor/manuscript/StoryManuscriptSidebar.tsx
    - components/editor/PreviewScreen.tsx
    - components/editor/TimelinePanel.tsx
  </files>
  <read_first>
    - lib/_core/theme.ts:172-211 (withAlpha signature: `(color: string | undefined, alpha: number) => string`)
    - The current target file (read it before editing)
  </read_first>
  <action>
    Apply the following mechanical replacements. For each file, FIRST add `import { withAlpha } from '@/lib/_core/theme';` at the top (merge into the existing import-from-`@/lib/_core/theme` block, or add a new import line). THEN perform the edits below.

    File 1: `components/document-editor/DocumentSceneSidebar.tsx:30`
    - Replace: `backgroundColor: scene.id === activeSceneId ? \`${colors.primary}12\` : colors.background,`
    - With:    `backgroundColor: scene.id === activeSceneId ? withAlpha(colors.primary, 0x12 / 255) : colors.background,`

    File 2: `components/document-editor/DocumentBlockDialogue.tsx:97,98`
    - Line 97 replace: `backgroundColor: isPhone ? 'transparent' : \`${colors.primary}14\`,` → `backgroundColor: isPhone ? 'transparent' : withAlpha(colors.primary, 0x14 / 255),`
    - Line 98 replace: `borderColor: isPhone ? 'transparent' : \`${colors.primary}55\`,` → `borderColor: isPhone ? 'transparent' : withAlpha(colors.primary, 0x55 / 255),`

    File 3: `components/document-editor/DocumentCommandMenu.tsx:104`
    - Replace: `backgroundColor: \`${toneColor}16\`,` → `backgroundColor: withAlpha(toneColor, 0x16 / 255),`

    File 4: `components/document-editor/DocumentChip.tsx:35,36`
    - Line 35 replace: `borderColor: selected ? colors.primary : block.warning ? colors.warning : \`${toneColor}40\`,` → `borderColor: selected ? colors.primary : block.warning ? colors.warning : withAlpha(toneColor, 0x40 / 255),`
    - Line 36 replace: `backgroundColor: selected ? \`${colors.primary}14\` : \`${toneColor}12\`,` → `backgroundColor: selected ? withAlpha(colors.primary, 0x14 / 255) : withAlpha(toneColor, 0x12 / 255),`

    File 5: `components/editor/manuscript/StoryManuscriptBlock.tsx:303`
    - Replace: `backgroundColor: \`${colors.primary}14\`,` → `backgroundColor: withAlpha(colors.primary, 0x14 / 255),`

    File 6: `components/editor/manuscript/StoryManuscriptSidebar.tsx:57`
    - Replace: `backgroundColor: isActive ? \`${colors.primary}18\` : colors.background,` → `backgroundColor: isActive ? withAlpha(colors.primary, 0x18 / 255) : colors.background,`

    File 7: `components/editor/PreviewScreen.tsx:303` (NOTE: this file has only 299 lines; the audit refers to a prior version. Verify the location — if no longer present, skip this entry and document the skip in the commit body).
    - IF present: replace `\`${colors.primary}14\`` with `withAlpha(colors.primary, 0x14 / 255)`.
    - If absent: this site was already migrated; record in the commit body "PreviewScreen.tsx hex+alpha already removed in prior refactor; CR-2 WR-1 site at line 303 no longer present".

    File 8: `components/editor/TimelinePanel.tsx:169`
    - Replace: `shadowColor: '#000',` → `shadowColor: colors.foreground,`
    - Add `import { useColors } from '@/hooks/use-colors';` if not present, and `const colors = useColors();` if not present. If `colors` is already in scope from existing hooks, just use it.

    Alpha conversion table for the `0xNN / 255` literal:
    0x12 → 0.0706 (use 0.07 to match prior M2 fix `withAlpha(colors.primary, 0.07)` for `12`)
    0x14 → 0.08  (matches M2 fix; use 0.08)
    0x16 → 0.09
    0x18 → 0.09
    0x40 → 0.25
    0x55 → 0.33
  </action>
  <verify>
    <automated>grep -rn '\${colors\.[a-z0-9_]*}[0-9a-fA-F]\{2\}\|\${toneColor}[0-9a-fA-F]\{2\}' components/ app/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __mocks__ | grep -v __tests__</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 matches across `components/` and `app/` (excluding node_modules, __mocks__, __tests__)
    - All 8 files contain `import { withAlpha }` from `@/lib/_core/theme`
    - `pnpm check` (tsc --noEmit) passes — no type errors
  </acceptance_criteria>
  <done>
    All hex+alpha string-concat sites in document-editor/, manuscript/, and editor/ are replaced with withAlpha() calls. TimelinePanel uses colors.foreground for shadowColor.
  </done>
</task>

<task type="auto">
  <name>Task 3 (CR-3): Replace hardcoded color fallbacks with null contract</name>
  <files>lib/story-reader-platform.ts</files>
  <read_first>
    - lib/story-reader-platform.ts (target file)
    - components/story-reader-responsive.tsx (consumer — must check the call sites still work)
    - lib/_core/theme.ts (RuntimePalette — has colors.foreground / colors.background already typed)
  </read_first>
  <action>
    In `lib/story-reader-platform.ts`:
    1. Change line 7: `backgroundColor: colors?.background ?? '#000000',` → `backgroundColor: colors?.background ?? null,`
    2. Change line 17: `color: colors?.foreground ?? '#ffffff',` → `color: colors?.foreground ?? null,`
    3. Update return type of `getStoryReaderContainerStyle` from `Pick<ViewStyle, 'backgroundColor' | 'overflow'>` to `Pick<ViewStyle, 'backgroundColor' | 'overflow'> & { backgroundColor: string | null }` (or use a simpler `backgroundColor: string | null` override — RN's `backgroundColor` accepts `null` at runtime; the type system may need an explicit cast).
    4. If TS errors arise (RN `backgroundColor` type is `string | OpaqueColorValue`), cast to `any` for the null branch only with a one-line comment: `// null is intentional: consumer must pass colors explicitly per platform contract`.
    5. Verify consumer in `components/story-reader-responsive.tsx` ALWAYS passes `colors` — if any call site passes `undefined`, the null fallback activates and the consumer must handle it (e.g., spread into style with a guard). Search for `getStoryReaderContainerStyle` and `getStoryReaderSpeakerTextStyle` calls.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `lib/story-reader-platform.ts:7` does NOT contain the literal string `'#000000'`
    - `lib/story-reader-platform.ts:17` does NOT contain the literal string `'#ffffff'`
    - `lib/story-reader-platform.ts` contains the literal `: null,` (with the comma) on both line 7 and 17
    - `grep -n "fallback\|'#000\|'#fff" lib/story-reader-platform.ts` returns 0 matches
    - `pnpm check` passes (TypeScript clean)
  </acceptance_criteria>
  <done>
    Hardcoded black/white fallbacks removed; the platform contract is now null (consumer must pass colors). TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 4 (CR-4): Extend IconSymbol MAPPING and replace 7 Unicode glyph sites</name>
  <files>
    - components/ui/icon-symbol.tsx
    - components/SplashScreenEditor.tsx
    - components/InteractiveObjectsEditor.tsx
    - components/editor/PreviewScreen.tsx
    - components/editor/SceneSelector.tsx
    - components/editor/SceneComposerPhone.tsx
  </files>
  <read_first>
    - components/ui/icon-symbol.tsx (target; the MAPPING object on line 13-60)
    - The current target file for each of the 5 consumer files (read before editing)
    - @expo/vector-icons/MaterialIcons names list (use Context7 `resolve-library-id` for `material-icons` if unsure of the exact Material name)
  </read_first>
  <action>
    Step A — Extend MAPPING in `components/ui/icon-symbol.tsx` (insert before the closing `} satisfies Record<string, ...>` on line 60):
    ```typescript
      "chevron.up": "keyboard-arrow-up",
      "chevron.down": "keyboard-arrow-down",
      "arrow.left": "arrow-back",
      "arrow.right": "arrow-forward",
      "xmark": "close",
      "checkmark": "check",
      "plus.circle": "add-circle",
      "minus": "remove",
      "square.and.arrow.up": "ios-share",
    ```
    Use these exact Material icon names (verified equivalents for the SF Symbols). If Context7 shows different names, prefer the verified ones.

    Step B — Replace Unicode glyphs in the 5 consumer files. For each, FIRST ensure `IconSymbol` is imported (if not already), THEN replace the glyph character with `<IconSymbol name="..." size={...} color={...} />`.

    File 1: `components/SplashScreenEditor.tsx:122`
    - Find: `{expanded ? '▲' : '▼'}` inside a Pressable/text element
    - Replace with: `<IconSymbol name={expanded ? 'chevron.up' : 'chevron.down'} size={16} color={colors.foreground} />`
    - Add `import { IconSymbol } from '@/components/ui/icon-symbol';` if not present.

    File 2: `components/SplashScreenEditor.tsx:372`
    - Find: `<Text ...>✓</Text>`
    - Replace with: `<IconSymbol name="checkmark" size={14} color={colors['text-inverse']} />`
    - Wrap in the same Pressable that contains the surrounding conditional.

    File 3: `components/InteractiveObjectsEditor.tsx:124`
    - Find: `{expanded ? '▲' : '▼'}`
    - Replace with: `<IconSymbol name={expanded ? 'chevron.up' : 'chevron.down'} size={16} color={colors.foreground} />`
    - Add import if not present.

    File 4: `components/editor/PreviewScreen.tsx:280`
    - Find: `<Text ...>← {t('menu.back')}</Text>`
    - Replace the `←` glyph with `<IconSymbol name="arrow.left" size={14} color={colors['text-inverse']} />` placed BEFORE the `<Text>` content (or merge into one Pressable child). Keep `t('menu.back')` text. Add `import { IconSymbol } from '@/components/ui/icon-symbol';` if not present (line 15 already imports it).

    File 5: `components/editor/SceneSelector.tsx:390`
    - Find: `{' → '}` between scene names
    - Replace with: `<IconSymbol name="arrow.right" size={14} color={colors.muted} />`
    - Wrap in a small container or fragment; the literal ' → ' is a separator between `<Text>` elements.

    File 6: `components/editor/SceneSelector.tsx:518`
    - Find: `▼ {input}` pattern
    - Replace `▼` with `<IconSymbol name="chevron.down" size={12} color={colors.muted} />` placed before the text.

    File 7: `components/editor/SceneComposerPhone.tsx:191`
    - Find: `<IconSymbol name="chevron.right" size={22} color={colors.foreground} style={rotatedBackIconStyle} />`
    - Replace with: `<IconSymbol name="arrow.left" size={22} color={colors.foreground} />`
    - Delete the `const rotatedBackIconStyle = useMemo(() => ({ transform: [{ rotate: '180deg' }] }), []);` declaration on line 178 if it becomes unused (verify with grep first). If `rotatedBackIconStyle` is referenced anywhere else, leave it.
  </action>
  <verify>
    <automated>grep -rP '[\xE2\x96\xB6\xE2\x97\x80\xE2\x86\x90\xE2\x86\x92\xE2\x9C\x93]' components/ app/ --include='*.tsx' --include='*.ts' 2>/dev/null | grep -v __tests__ | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - The grep for Unicode glyphs `▲▼←→✓` in components/ and app/ returns 0 matches (excluding `LanguageSelector.tsx` flag emojis which are allowed per UI-SPEC §P0)
    - `components/ui/icon-symbol.tsx` contains all 9 new keys: `chevron.up`, `chevron.down`, `arrow.left`, `arrow.right`, `xmark`, `checkmark`, `plus.circle`, `minus`, `square.and.arrow.up`
    - `pnpm check` passes
  </acceptance_criteria>
  <done>
    IconSymbol MAPPING extended; all Unicode glyph controls replaced with semantic icons. UI-SPEC §P0 Pillar 2 violations for these 6 sites are resolved.
  </done>
</task>

<task type="auto">
  <name>Task 5 (CR-5): Add 21+ translation keys and replace hardcoded English a11y/UI strings</name>
  <files>
    - lib/translations.ts
    - app/preview.tsx
    - components/editor/MiniPreview.tsx
    - components/story-reader-responsive.tsx
    - components/reader/ReaderControls.tsx
    - components/reader/ReaderChoices.tsx
    - components/editor/properties/panel-chrome.tsx
    - components/editor/properties/asset-field.tsx
    - components/document-editor/DocumentBlockChoice.tsx
    - components/document-editor/DocumentBlockDialogue.tsx
    - components/document-editor/DocumentPage.tsx
    - components/SplashScreenEditor.tsx
  </files>
  <read_first>
    - lib/translations.ts (target; EN map on lines 20-490ish, UK map on lines 491-980ish — find by searching for a known key like 'common.loading')
    - lib/i18n.ts (t() API: signature `(key, params?, fallback?) => string`, uses `{name}` interpolation)
    - Each target file to be modified (read the lines mentioned before editing)
  </read_first>
  <action>
    Step A — Add these keys to the EN map (`const EN: Record<string, string> = { ... }`) in `lib/translations.ts`. The EN map already contains `'common.loading': 'Loading...'` (line 146) and `'document.invalidRoute': 'Cannot open this story route'` (line 385). Do NOT duplicate these.

    Add (or verify present) in EN map:
    ```
      'common.noRouteProvided': 'No story or scene ID provided',
      'splash.configured': 'Splash configured',
      'splash.status': 'Splash: {duration}ms, {type}',
      'splash.statusUnitMs': 'ms',
      'reader.continueHint': 'Tap to advance text or make a choice',
      'reader.hints.back': 'Double tap to go back',
      'reader.hints.menu': 'Double tap to open menu',
      'reader.hints.save': 'Double tap to save',
      'reader.hints.autoPlay': 'Double tap to toggle auto-play mode',
      'reader.hints.history': 'Double tap to open dialogue history',
      'reader.hints.fastForward': 'Hold to fast-forward through text',
      'reader.hints.selectChoice': 'Double tap to select this choice',
      'editor.properties.closeHint': 'Close properties panel',
      'editor.properties.duplicateHint': 'Duplicate this block',
      'editor.properties.deleteHint': 'Delete this block from timeline',
      'editor.properties.assetFieldHint': 'Enter asset ID or tap search to browse',
      'editor.selectX': 'Select {category}...',
      'document.choice.addOption': 'Add choice option',
      'document.choice.removeOption': 'Remove choice',
      'document.choice.editOption': 'Edit choice',
      'document.choice.moveOption': 'Move choice',
      'document.choice.questionLabel': 'Choice question',
      'document.choice.optionLabel': 'Choice option {number}',
      'document.dialogue.characterHint': 'Character name for this dialogue line',
      'document.dialogue.lineHint': 'Dialogue text for this character',
      'document.a11y.sceneTitle': 'Scene title',
      'document.a11y.narration': 'Narration or story text',
      'document.a11y.slashCommandInput': 'Type / for commands, Enter to add line',
    ```

    Step B — Mirror the same keys in the UK map (`const UK: Record<string, string> = { ... }`) with Ukrainian translations. Use the same key names. Example translations (verify with native speaker or use a translation reference; if unsure, use English as placeholder but DO commit all 30 keys with their UA versions):
    - `'common.noRouteProvided': 'Не вказано ID історії або сцени'`
    - `'splash.configured': 'Заставку налаштовано'`
    - `'splash.status': 'Заставка: {duration} мс, {type}'`
    - `'splash.statusUnitMs': 'мс'`
    - `'reader.continueHint': 'Торкніться, щоб продовжити текст або зробити вибір'`
    - `'reader.hints.back': 'Двічі торкніться, щоб повернутися'`
    - `'reader.hints.menu': 'Двічі торкніться, щоб відкрити меню'`
    - `'reader.hints.save': 'Двічі торкніться, щоб зберегти'`
    - `'reader.hints.autoPlay': 'Двічі торкніться, щоб увімкнути авто-читання'`
    - `'reader.hints.history': 'Двічі торкніться, щоб відкрити історію діалогів'`
    - `'reader.hints.fastForward': 'Утримуйте, щоб прокрутити текст уперед'`
    - `'reader.hints.selectChoice': 'Двічі торкніться, щоб обрати цей варіант'`
    - `'editor.properties.closeHint': 'Закрити панель властивостей'`
    - `'editor.properties.duplicateHint': 'Дублювати цей блок'`
    - `'editor.properties.deleteHint': 'Видалити цей блок зі шкали часу'`
    - `'editor.properties.assetFieldHint': 'Введіть ID активу або торкніться пошуку'`
    - `'editor.selectX': 'Обрати {category}...'`
    - `'document.choice.addOption': 'Додати варіант вибору'`
    - `'document.choice.removeOption': 'Видалити вибір'`
    - `'document.choice.editOption': 'Редагувати вибір'`
    - `'document.choice.moveOption': 'Перемістити вибір'`
    - `'document.choice.questionLabel': 'Питання вибору'`
    - `'document.choice.optionLabel': 'Варіант вибору {number}'`
    - `'document.dialogue.characterHint': "Ім'я персонажа для цього рядка діалогу"`
    - `'document.dialogue.lineHint': 'Текст діалогу для цього персонажа'`
    - `'document.a11y.sceneTitle': 'Назва сцени'`
    - `'document.a11y.narration': 'Оповідання або текст історії'`
    - `'document.a11y.slashCommandInput': 'Введіть / для команд, Enter щоб додати рядок'`

    Step C — Replace hardcoded English a11y strings. For each line listed below, replace the literal string with `t('<key>')` (using the keys from Step A/B). If the line uses `accessibilityLabel={`...`}` with template interpolation (e.g. `Choice option ${index + 1}`), use `accessibilityLabel={t('document.choice.optionLabel', { number: index + 1 })}`. If the call site does not already import `useI18n`, add the import and destructure `t` at the top of the file.

    Specific line replacements:
    - `app/preview.tsx:18` — handled in CR-6 task (uses common.loading).
    - `app/preview.tsx:26` — handled in CR-6 task.
    - `components/editor/MiniPreview.tsx:170` — `Loading...` → `{t('common.loading')}`
    - `components/story-reader-responsive.tsx:270` — `continueAccessibilityHint="Tap to advance text or make a choice"` → `continueAccessibilityHint={t('reader.continueHint')}`
    - `components/reader/ReaderControls.tsx:89` — `accessibilityHint="Double tap to toggle auto-play mode"` → `accessibilityHint={t('reader.hints.autoPlay')}`
    - `components/reader/ReaderControls.tsx:96` — `accessibilityHint="Double tap to open dialogue history"` → `accessibilityHint={t('reader.hints.history')}`
    - `components/reader/ReaderControls.tsx:119` — `accessibilityHint="Hold to fast-forward through text"` → `accessibilityHint={t('reader.hints.fastForward')}`
    - `components/reader/ReaderChoices.tsx:40` — `accessibilityHint="Double tap to select this choice"` → `accessibilityHint={t('reader.hints.selectChoice')}`
    - `components/editor/properties/panel-chrome.tsx:55` — `accessibilityHint="Close properties panel"` → `accessibilityHint={t('editor.properties.closeHint')}`
    - `components/editor/properties/panel-chrome.tsx:91` — `accessibilityHint="Duplicate this block"` → `accessibilityHint={t('editor.properties.duplicateHint')}`
    - `components/editor/properties/panel-chrome.tsx:105` — `accessibilityHint="Delete this block from timeline"` → `accessibilityHint={t('editor.properties.deleteHint')}`
    - `components/editor/properties/asset-field.tsx:36` — `placeholder={\`Select ${category}...\`}` → `placeholder={t('editor.selectX', { category })}`
    - `components/editor/properties/asset-field.tsx:39` — `accessibilityHint="Enter asset ID or tap search to browse"` → `accessibilityHint={t('editor.properties.assetFieldHint')}`
    - `components/document-editor/DocumentBlockChoice.tsx:66` — `accessibilityLabel="Choice question"` → `accessibilityLabel={t('document.choice.questionLabel')}`
    - `components/document-editor/DocumentBlockChoice.tsx:67` — `accessibilityHint="Question text for the player to choose from"` → `accessibilityHint={t('document.choice.addOption')}` (closest intent)
    - `components/document-editor/DocumentBlockChoice.tsx:93` — `accessibilityLabel={\`Choice option \${index + 1}\`}` → `accessibilityLabel={t('document.choice.optionLabel', { number: index + 1 })}`
    - `components/document-editor/DocumentBlockChoice.tsx:94` — `accessibilityHint="Text for this answer choice"` → `accessibilityHint={t('document.choice.editOption')}`
    - `components/document-editor/DocumentBlockDialogue.tsx:110` — `accessibilityHint="Character name for this dialogue line"` → `accessibilityHint={t('document.dialogue.characterHint')}`
    - `components/document-editor/DocumentBlockDialogue.tsx:136` — `accessibilityHint="Dialogue text for this character"` → `accessibilityHint={t('document.dialogue.lineHint')}`
    - `components/document-editor/DocumentPage.tsx:138` — `accessibilityHint="Scene title"` → `accessibilityHint={t('document.a11y.sceneTitle')}`
    - `components/document-editor/DocumentPage.tsx:219` — `accessibilityHint="Narration or story text"` → `accessibilityHint={t('document.a11y.narration')}`
    - `components/document-editor/DocumentPage.tsx:270` — `accessibilityHint="Type / for commands, Enter to add line"` → `accessibilityHint={t('document.a11y.slashCommandInput')}`
    - `components/SplashScreenEditor.tsx:387` — `Splash configured • {splash?.duration}ms • {splash?.type}` → `{t('splash.status', { duration: splash?.duration, type: splash?.type })}` (use a space separator, not `•`).

    For every file that uses `t()` but doesn't import it, add:
    ```typescript
    import { useI18n } from '@/lib/i18n';
    // then inside the component:
    const { t } = useI18n();
    ```
  </action>
  <verify>
    <automated>grep -rPn 'accessibilityHint="[A-Z]|continueAccessibilityHint="[A-Z]|placeholder="Select ' components/ app/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v __tests__</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 matches across components/ and app/ (excluding node_modules, __tests__, mocks)
    - `lib/translations.ts` EN map contains all 27 keys listed in Step A (some may already be present from prior phases; verify with `grep -E "'(common\.loading|common\.noRouteProvided|splash\.configured|splash\.status|reader\.continueHint|reader\.hints\.[a-z]+|editor\.properties\.[a-z]+|editor\.selectX|document\.choice\.[a-z]+|document\.dialogue\.[a-z]+|document\.a11y\.[a-z]+)'" lib/translations.ts` — must return at least 27 matches)
    - `pnpm check` passes
    - All `t()`-using files import `useI18n` from `@/lib/i18n`
  </acceptance_criteria>
  <done>
    21+ hardcoded English a11y/UI strings are localized. EN and UK maps contain all required keys. Pillar 1 (Copywriting) WARNINGs resolved.
  </done>
</task>

<task type="auto">
  <name>Task 6 (CR-6): app/preview.tsx dead-end error state with recovery action</name>
  <files>app/preview.tsx</files>
  <read_first>
    - app/preview.tsx (target; full file is 32 lines)
    - lib/translations.ts (verify keys: 'common.loading', 'common.back', 'document.invalidRoute')
    - expo-router `useRouter`, `router.back()` API (use Context7 if needed)
  </read_first>
  <action>
    Rewrite `app/preview.tsx` (32 lines) to:
    1. Add imports: `ActivityIndicator, Pressable` from 'react-native'; `useRouter` from 'expo-router'; `useI18n` from '@/lib/i18n'.
    2. Inside the component, call `const router = useRouter();` and `const { t } = useI18n();`.
    3. Replace the loading state (lines 16-21) with:
       ```tsx
       <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.background, gap: spacing.md }}>
         <ActivityIndicator size="large" color={colors.primary} />
         <Text style={{ color: colors.foreground, ...typeScale.body }}>{t('common.loading')}</Text>
       </View>
       ```
       (use `useColors()` for colors, `typeScale.body` from `@/lib/design-tokens` for the text size, `spacing.md` for gap).
    4. Replace the error state (lines 24-28) with:
       ```tsx
       <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.background, gap: spacing.md, padding: spacing.lg }}>
         <Text style={{ color: colors.foreground, ...typeScale.sectionTitle, textAlign: 'center' }}>{t('document.invalidRoute')}</Text>
         <Pressable
           onPress={() => router.back()}
           accessibilityRole="button"
           accessibilityLabel={t('menu.back')}
           style={({ pressed }) => ({
             paddingHorizontal: spacing.lg,
             paddingVertical: spacing.md,
             backgroundColor: colors.primary,
             borderRadius: radius.md,
             opacity: pressed ? 0.7 : 1,
           })}
         >
           <Text style={{ color: colors['text-inverse'], ...typeScale.label }}>{t('menu.back')}</Text>
         </Pressable>
       </View>
       ```
    5. Add `useColors` import from `@/hooks/use-colors` and call `const colors = useColors();` inside the component.
  </action>
  <verify>
    <automated>grep -n "Loading\.\.\.\|No story or scene ID provided" app/preview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 matches
    - `app/preview.tsx` contains `<ActivityIndicator` (line 18 area)
    - `app/preview.tsx` contains `{t('common.loading')}` and `{t('document.invalidRoute')}`
    - `app/preview.tsx` contains `<Pressable` and `onPress={() => router.back()}` (or equivalent back navigation)
    - `pnpm check` passes
  </acceptance_criteria>
  <done>
    app/preview.tsx shows a spinner + t('common.loading') while loading, and a recovery button + t('document.invalidRoute') for invalid routes. No dead-ends.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7 (CR-7): Move story-manuscript-save dedup out of __DEV__ guard</name>
  <files>lib/editor/story-manuscript-save.ts, __tests__/unit/lib/story-manuscript-save.test.ts</files>
  <read_first>
    - lib/editor/story-manuscript-save.ts (target; lines 60-104 contain the bug)
    - __tests__/unit/lib/story-manuscript-save.test.ts (existing test pattern — extends with a new test for production dedup)
  </read_first>
  <behavior>
    - Test: when `__DEV__` is false (simulate by setting globalThis.__DEV__ = false before calling `applyStoryManuscriptChanges`), passing two manuscript blocks with the same `sourceStepId` should result in the second being SKIPPED (the original `originalStep` is not duplicated into the timeline).
    - The existing test must continue to pass (dev-mode behavior unchanged).
  </behavior>
  <action>
    In `lib/editor/story-manuscript-save.ts`:
    1. Locate the `if (__DEV__) { ... }` block on lines 75-88.
    2. Move the dedup logic OUT of the `__DEV__` block. The new structure:
       ```typescript
       const nextTimeline = sceneSection.blocks.flatMap((block) => {
         // Dedup runs in both dev and production to prevent duplicate timeline entries.
         if (seenSourceStepIds.has(block.sourceStepId)) {
           if (__DEV__) {
             console.warn(
               `[applyStoryManuscriptChanges] Duplicate sourceStepId "${block.sourceStepId}" in scene "${sceneRecord.id}" — multiple manuscript blocks reference the same timeline step`
             );
           }
           return []; // skip duplicate
         }
         seenSourceStepIds.add(block.sourceStepId);

         if (__DEV__) {
           if (!originalStepsById.has(block.sourceStepId)) {
             console.warn(
               `[applyStoryManuscriptChanges] Orphan block "${block.id}" in scene "${sceneRecord.id}" — sourceStepId "${block.sourceStepId}" not found. A new step will be auto-created.`
             );
           }
         }

         const originalStep = originalStepsById.get(block.sourceStepId);
         if (!originalStep) {
           return [createStepFromManuscriptBlock(block)];
         }

         return [updateStepFromManuscriptBlock(originalStep, block)];
       });
       ```
    3. Note: the previous code returned the original step (line 95 in the old file) even when a duplicate was detected (because the `__DEV__` block only WARNED but didn't return). The fix correctly returns `[]` for duplicates.

    In `__tests__/unit/lib/story-manuscript-save.test.ts`:
    1. Add a new `describe('production dedup')` block.
    2. Set `globalThis.__DEV__ = false;` in `beforeEach` of that block, restore in `afterEach`.
    3. Add a test that creates a manuscript with two blocks having the same `sourceStepId`, calls `applyStoryManuscriptChanges`, and asserts the resulting `sceneRecords[0].timeline` does NOT contain the duplicated step.
  </action>
  <verify>
    <automated>pnpm test __tests__/unit/lib/story-manuscript-save.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test __tests__/unit/lib/story-manuscript-save.test.ts` exits 0; all original tests + the new "production dedup" test pass
    - `lib/editor/story-manuscript-save.ts` line 75 area contains `if (seenSourceStepIds.has(block.sourceStepId)) {` OUTSIDE the `if (__DEV__)` block (verify with `grep -B 2 -A 2 "seenSourceStepIds.has" lib/editor/story-manuscript-save.ts` — the `has` check should appear before the `if (__DEV__) {` line)
    - The `if (__DEV__)` block does NOT contain `seenSourceStepIds.add(sourceStepId)` or `seenSourceStepIds.has(sourceStepId)`
  </acceptance_criteria>
  <done>
    Dedup logic in story-manuscript-save.ts runs in production builds, not only in __DEV__. New test verifies the production path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → theme | Components import `withAlpha` from theme; if the helper is broken, alpha rendering is wrong |
| component → i18n | Components read `t()` from translations; missing keys fall back to English via lib/i18n.ts:22-29 |
| native reader → platform | story-reader-platform.ts exports pure functions; consumers must pass `colors` explicitly after CR-3 |
| dev vs prod build | `__DEV__` is a global boolean; business logic that depends on it silently fails in release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | npm install | mitigate | Use existing deps only; no new packages |
| T-15-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-15-01-1 | T (Tampering) | useTypewriter textSpeed | mitigate | Unit test verifies live read of textSpeedRef; CR-1 test asserts mid-typing change |
| T-15-01-2 | T (Tampering) | story-manuscript-save dedup | mitigate | Unit test with `__DEV__=false` verifies dedup runs in production |
| T-15-01-3 | I (Information Disclosure) | lib/story-reader-platform.ts fallbacks | accept | Null fallbacks are intentional; consumers must pass colors; low risk (no secrets) |
| T-15-01-4 | R (Repudiation) | Production __DEV__-gated logic | mitigate | CR-7 removes business logic from __DEV__; ESLint rule to be added in Wave 3 |
| T-15-01-5 | E (Elevation of Privilege) | Hardcoded color literals | mitigate | withAlpha migration centralizes alpha computation in lib/_core/theme.ts |
</threat_model>

<verification>
- `pnpm test` — full unit test suite passes (40+ tests); new useTypewriter + story-manuscript tests included
- `pnpm check` (tsc --noEmit) — no type errors
- `pnpm lint` — no new warnings
- `grep -rn '\${colors\.[a-z0-9_]*}[0-9a-fA-F]\{2\}\|\${toneColor}[0-9a-fA-F]\{2\}' components/ app/ --include='*.tsx' --include='*.ts'` — 0 matches
- `grep -rPn 'accessibilityHint="[A-Z]|continueAccessibilityHint="[A-Z]' components/ app/` — 0 matches
- `grep -rP '[\xE2\x96\xB6\xE2\x97\x80\xE2\x86\x90\xE2\x86\x92\xE2\x9C\x93]' components/ app/ --include='*.tsx'` — 0 matches in non-LanguageSelector files
- `lib/story-reader-platform.ts` does not contain `'#000000'` or `'#ffffff'`
- `app/preview.tsx` does not contain `Loading...` or `No story or scene ID provided`
</verification>

<success_criteria>
- All 7 critical findings from the 2026-06-04 deep audit are resolved
- Beta release is unblocked: no user-visible bugs, no P0 UI-SPEC violations, no production-only data loss
- 7 atomic commits land in the format `fix(15-01): CR-N <description>` per AGENTS.md conventions
- `pnpm check`, `pnpm test`, `pnpm lint` all pass
- Each requirement ID (CR-1..CR-7) is traceable to a task and a commit
</success_criteria>

<output>
Create `.planning/phases/15-deep-audit-remediation/15-01-SUMMARY.md` when done
</output>
