---
phase: 01-canonical-scene-model
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/story-hooks.ts
  - app/reader.tsx
  - hooks/useReaderInitialization.ts
  - __tests__/unit/lib/story-hooks-canonical-scene.test.ts
  - __tests__/unit/stores/use-app-store-canonical.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Current story reconstruction can read canonical scene data instead of depending only on scenesByStory"
    - "Legacy scene fallback, if still required, flows through the centralized adapter rather than ad hoc shape reconstruction"
    - "Targeted automated tests prove canonical scene selectors and story reconstruction work for the transition state"
  artifacts:
    - path: lib/story-hooks.ts
      provides: Canonical-first story reconstruction and story-facing selectors
      contains: "sceneRecordsByStory"
    - path: __tests__/unit/lib/story-hooks-canonical-scene.test.ts
      provides: Coverage for canonical story reconstruction
      min_lines: 25
    - path: __tests__/unit/stores/use-app-store-canonical.test.ts
      provides: Coverage for canonical scene selectors or metadata-preserving helpers
      min_lines: 25
  key_links:
    - from: lib/story-hooks.ts
      to: stores/use-app-store.ts
      via: canonical scene selectors
      pattern: "selectCanonicalSceneRecord|selectSceneRecordsForStory"
    - from: app/reader.tsx
      to: lib/story-hooks.ts
      via: current story reconstruction or canonical story access
      pattern: "useStoryState|currentStory"
---

<objective>
Push the canonical scene contract through story reconstruction and the runtime-facing boundary without attempting the full reader refactor yet.

Purpose: Make the rest of the application capable of reading the canonical scene path so later editor and runtime phases are additive rather than architectural rewrites.
Output: Canonical-first story hooks, narrowed runtime assumptions, and targeted tests for the transition path.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-scene-model/01-CONTEXT.md
@.planning/phases/01-canonical-scene-model/01-01-PLAN.md
@lib/story-hooks.ts
@stores/use-app-store.ts
@lib/scene-record-adapter.ts
@app/reader.tsx
@hooks/useReaderInitialization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make story reconstruction canonical-first with controlled legacy fallback</name>
  <files>lib/story-hooks.ts, stores/use-app-store.ts, lib/scene-record-adapter.ts</files>
  <read_first>lib/story-hooks.ts, stores/use-app-store.ts, lib/scene-record-adapter.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, assets/demo-story.json</read_first>
  <action>Refactor `lib/story-hooks.ts` so current story reconstruction and exported story-facing helpers can read canonical scene records first. Where a `Story` object or legacy `StoryScene` map is still required, build it through the centralized adapter instead of recreating compatibility shapes inline. Preserve the demo story special-case if it is still required, but isolate the branch so canonical persisted stories do not depend only on `scenesByStory` anymore.</action>
  <verify>Run `pnpm check` and inspect `lib/story-hooks.ts` for canonical-first scene access.</verify>
  <acceptance_criteria>
    - `lib/story-hooks.ts` references `sceneRecordsByStory`
    - `lib/story-hooks.ts` no longer reconstructs persisted stories solely from `scenesByStory`
    - Any legacy conversion in `lib/story-hooks.ts` uses `lib/scene-record-adapter.ts`
  </acceptance_criteria>
  <done>Story reconstruction can source scene data from the canonical model while keeping a controlled compatibility path.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow runtime-facing code to the new scene boundary</name>
  <files>app/reader.tsx, hooks/useReaderInitialization.ts, lib/story-hooks.ts</files>
  <read_first>app/reader.tsx, hooks/useReaderInitialization.ts, lib/story-hooks.ts, stores/use-app-store.ts, .planning/phases/01-canonical-scene-model/01-CONTEXT.md</read_first>
  <action>Update `app/reader.tsx` and `hooks/useReaderInitialization.ts` only as much as needed so they consume the canonicalized story reconstruction path without assuming that persisted scene data lives exclusively in `scenesByStory`. Do not attempt the full runtime unification from Phase 3 here. The goal is to narrow direct legacy assumptions and ensure these consumers can operate when canonical scene records are present.</action>
  <verify>Run `pnpm check` and verify there are no direct new assumptions tying runtime initialization only to `scenesByStory`.</verify>
  <acceptance_criteria>
    - `app/reader.tsx` compiles against the updated story hook contract
    - `hooks/useReaderInitialization.ts` compiles against the updated story hook contract
    - No new direct read of `scenesByStory` is introduced in these files for persisted canonical scenes
  </acceptance_criteria>
  <done>The runtime-facing boundary is compatible with the canonical scene path without expanding phase scope into full playback refactors.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for canonical selectors and story reconstruction</name>
  <files>__tests__/unit/lib/story-hooks-canonical-scene.test.ts, __tests__/unit/stores/use-app-store-canonical.test.ts, lib/story-hooks.ts, stores/use-app-store.ts</files>
  <read_first>vitest.config.ts, __tests__/unit/**/*.test.ts, lib/story-hooks.ts, stores/use-app-store.ts, lib/scene-record-adapter.ts</read_first>
  <action>Create two targeted test files: one for canonical story reconstruction in `lib/story-hooks.ts`, and one for canonical selector or metadata-preserving helper behavior in `stores/use-app-store.ts`. Use the existing Vitest style in the repo, cover canonical-first read behavior, and include at least one regression assertion that would fail if the code fell back to legacy-only reconstruction when `sceneRecordsByStory` is present.</action>
  <verify>Run `pnpm test -- __tests__/unit/lib/story-hooks-canonical-scene.test.ts __tests__/unit/stores/use-app-store-canonical.test.ts` and then `pnpm check`.</verify>
  <acceptance_criteria>
    - `__tests__/unit/lib/story-hooks-canonical-scene.test.ts` exists
    - `__tests__/unit/stores/use-app-store-canonical.test.ts` exists
    - The tests assert canonical-first behavior when `sceneRecordsByStory` is populated
    - `pnpm check` passes after adding the tests
  </acceptance_criteria>
  <done>The transition path is protected by focused automated tests covering canonical reconstruction and selector behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- __tests__/unit/lib/story-hooks-canonical-scene.test.ts __tests__/unit/stores/use-app-store-canonical.test.ts` passes
- [ ] `pnpm check` passes
- [ ] Canonical-first reconstruction is visible in `lib/story-hooks.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Story reconstruction no longer depends only on the legacy scene map
- The transition to canonical scenes is protected by targeted tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-scene-model/01-02-SUMMARY.md`
</output>
