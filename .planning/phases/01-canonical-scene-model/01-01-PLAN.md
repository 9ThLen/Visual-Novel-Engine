---
phase: 01-canonical-scene-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCENE-MODEL-CONTRACT.md
  - lib/engine/types.ts
  - lib/types.ts
  - lib/scene-record-adapter.ts
  - stores/use-app-store.ts
autonomous: true
requirements:
  - ARCH-01
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Canonical scene model is defined explicitly as SceneRecord plus TimelineStep and documented in-repo"
    - "App store exposes canonical scene selectors and write helpers instead of relying on ad hoc direct reads from both scene maps"
    - "Legacy StoryScene conversion is centralized in one adapter module rather than spread across store and hooks"
  artifacts:
    - path: docs/SCENE-MODEL-CONTRACT.md
      provides: Canonical scene contract and compatibility boundary
      contains: "Canonical model: SceneRecord"
    - path: lib/scene-record-adapter.ts
      provides: Centralized conversion helpers between canonical and legacy scene shapes
      min_lines: 30
    - path: stores/use-app-store.ts
      provides: Canonical scene selectors and persistence helpers
      contains: "selectCanonicalSceneRecord"
  key_links:
    - from: stores/use-app-store.ts
      to: lib/scene-record-adapter.ts
      via: Canonical selector or helper import
      pattern: "scene-record-adapter"
---

<objective>
Establish the canonical scene contract and the store boundary that all later phases can build on.

Purpose: Remove ambiguity about which scene shape is the source of truth before editor and runtime refactors begin.
Output: A written scene contract, a centralized compatibility adapter, and canonical store helpers/selectors.
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-scene-model/01-CONTEXT.md
@stores/use-app-store.ts
@lib/engine/types.ts
@lib/types.ts
@lib/story-hooks.ts
@components/editor/SceneComposer.tsx
@app/reader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical scene contract and compatibility boundary</name>
  <files>docs/SCENE-MODEL-CONTRACT.md, lib/engine/types.ts, lib/types.ts</files>
  <read_first>docs/EDITOR-REDESIGN-PLAN.md, .planning/PROJECT.md, .planning/phases/01-canonical-scene-model/01-CONTEXT.md, lib/engine/types.ts, lib/types.ts, stores/use-app-store.ts, lib/story-hooks.ts</read_first>
  <action>Create `docs/SCENE-MODEL-CONTRACT.md` with these exact sections: `Canonical model`, `Compatibility model`, `Allowed conversions`, `Disallowed access patterns`, and `Phase boundaries`. State concretely that `SceneRecord + TimelineStep` is the canonical scene model, `StoryScene` is a temporary compatibility shape, and all new logic must read canonical scene data first. In `lib/engine/types.ts`, add or clarify canonical type aliases and comments so `SceneRecord`, `SceneConnection`, and timeline-bearing scene fields match the contract. In `lib/types.ts`, mark legacy scene types as compatibility-only with explicit deprecation guidance, without deleting fields that existing readers still need.</action>
  <verify>Check that the new contract doc exists and `pnpm check` passes after the type and comment updates.</verify>
  <acceptance_criteria>
    - `docs/SCENE-MODEL-CONTRACT.md` contains `Canonical model: SceneRecord`
    - `docs/SCENE-MODEL-CONTRACT.md` contains `Compatibility model: StoryScene`
    - `lib/types.ts` contains `compatibility-only` or `deprecated` guidance on the legacy scene path
    - `lib/engine/types.ts` still exports `SceneRecord` and `TimelineStep`
  </acceptance_criteria>
  <done>The repository has one explicit written scene contract and legacy types are clearly labeled as compatibility-only.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize legacy conversion in a single adapter module</name>
  <files>lib/scene-record-adapter.ts, lib/engine/types.ts, lib/types.ts</files>
  <read_first>lib/engine/types.ts, lib/types.ts, lib/story-hooks.ts, stores/use-app-store.ts, components/editor/SceneComposer.tsx, app/reader.tsx</read_first>
  <action>Add `lib/scene-record-adapter.ts` with explicit helpers for the transitional boundary, for example `sceneRecordToStoryScene(...)`, `storySceneToSceneRecord(...)` or a named draft/fallback variant if full round-trip is not safe yet. Keep the adapter one-directional where possible and document unsafe legacy-only fields instead of silently inventing data. Do not scatter conversion logic into store selectors or hooks in this task; all compatibility conversion must route through this one module.</action>
  <verify>Confirm the adapter file exports named conversion helpers and that `pnpm check` passes with the new imports/exports.</verify>
  <acceptance_criteria>
    - `lib/scene-record-adapter.ts` exists
    - `lib/scene-record-adapter.ts` contains `export function sceneRecordToStoryScene`
    - `lib/scene-record-adapter.ts` contains `export function` for a legacy-to-canonical conversion path or clearly named draft helper
    - No new conversion helper is added directly inside `stores/use-app-store.ts`
  </acceptance_criteria>
  <done>All compatibility conversion used by the codebase can point to one adapter module.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor the app store to expose canonical scene selectors and persistence helpers</name>
  <files>stores/use-app-store.ts, lib/scene-record-adapter.ts</files>
  <read_first>stores/use-app-store.ts, lib/scene-record-adapter.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>In `stores/use-app-store.ts`, add explicit canonical access helpers such as `selectCanonicalSceneRecord`, `selectSceneRecordsForStory`, and a write helper that preserves existing record metadata when updating scene content. The store may retain `scenesByStory` for compatibility during this phase, but canonical helpers must read from `sceneRecordsByStory` first and use the adapter module only when a compatibility representation is required. Avoid introducing new uncontrolled dual-write behavior; if legacy sync remains necessary, isolate it behind a named helper with a comment that Phase 3 will remove or narrow it.</action>
  <verify>Run `pnpm check` and inspect the store for the new selector/helper exports.</verify>
  <acceptance_criteria>
    - `stores/use-app-store.ts` contains `selectCanonicalSceneRecord`
    - `stores/use-app-store.ts` contains `selectSceneRecordsForStory`
    - `stores/use-app-store.ts` contains a metadata-preserving scene-record update helper
    - `stores/use-app-store.ts` imports from `lib/scene-record-adapter.ts` if compatibility conversion is still needed
  </acceptance_criteria>
  <done>The store exposes canonical scene access as first-class API and no new ad hoc scene-shape reads are introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm check` passes
- [ ] `docs/SCENE-MODEL-CONTRACT.md` exists and states the canonical scene model explicitly
- [ ] Store exports canonical selector/helpers for scene records
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical scene model is explicit in both code and documentation
- Store helpers make `sceneRecordsByStory` the intentional source for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-scene-model/01-01-SUMMARY.md`
</output>
