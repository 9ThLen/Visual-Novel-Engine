---
phase: 07-editor-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/ConfirmDialog.tsx
  - stores/use-editor-store.ts
  - app/scene-editor.tsx
autonomous: true
requirements:
  - POLISH-03
  - POLISH-04
  - POLISH-05
must_haves:
  truths:
    - "User sees a confirmation dialog before a block is deleted"
    - "User gets visual feedback (saving indicator) after saving a scene"
    - "Editor errors show a localized fallback UI with retry instead of crashing the whole app"
  artifacts:
    - path: "components/ui/ConfirmDialog.tsx"
      provides: "Reusable modal dialog for destructive action confirmation"
      exports: ["ConfirmDialog"]
      pattern: "export function ConfirmDialog"
    - path: "stores/use-editor-store.ts"
      provides: "isSaving flag for transient save feedback"
      contains: "isSaving"
    - path: "app/scene-editor.tsx"
      provides: "Editor-specific ErrorBoundary wrapping"
      contains: "<ErrorBoundary>"
  key_links:
    - from: "SceneComposer.tsx"
      to: "components/ui/ConfirmDialog.tsx"
      via: "import { ConfirmDialog }"
      pattern: "import.*ConfirmDialog"
    - from: "useEditorStore"
      to: "isSaving"
      via: "setIsSaving"
      pattern: "isSaving"
    - from: "app/scene-editor.tsx"
      to: "components/ErrorBoundary.tsx"
      via: "import { ErrorBoundary }"
      pattern: "<ErrorBoundary>"
---

<objective>
Create infrastructure components for Editor UX Polish: the ConfirmDialog reusable modal, isSaving state in the editor store, and ErrorBoundary wrapping for the editor route.

Purpose: These three pieces are pre-requisites for the SceneComposer integration in Plan 2. Each is independent and touches different files, enabling Wave 1 parallel execution.

Output:
- `components/ui/ConfirmDialog.tsx` — reusable destructive confirmation modal
- `stores/use-editor-store.ts` — add `isSaving` flag + `setIsSaving` action
- `app/scene-editor.tsx` — wrap `<SceneComposer>` with `<ErrorBoundary>`
</objective>

<execution_context>
@C:/Users/sidle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sidle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-editor-ux-polish/07-CONTEXT.md
@.planning/phases/07-editor-ux-polish/07-RESEARCH.md

<interfaces>
// From components/editor/modals/SaveSceneDialog.tsx — existing Modal pattern to replicate:
// Modal with animationType="fade" transparent, backdrop color, surface-container inner, header+body+footer

// From stores/use-editor-store.ts — store interface:
// Uses `create<EditorStore>()((set, get) => ({...}))` pattern
// All actions defined as interface methods
// Selectors exported as pure functions

// From components/ErrorBoundary.tsx — props:
// interface Props { children: ReactNode; colors?: RuntimePalette; fallback?: (error, errorInfo, reset) => ReactNode }
// Uses getDerivedStateFromError + componentDidCatch
// Has resetError method
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConfirmDialog component</name>
  <files>components/ui/ConfirmDialog.tsx</files>

  <read_first>
  # ConfirmDialog contract from CONTEXT.md:
  # - visible: boolean, title: string, message: string
  # - confirmLabel?: string (default "Delete"), cancelLabel?: string (default "Cancel")
  # - onConfirm: () => void, onCancel: () => void
  # - destructive?: boolean (default true, highlights confirm as destructive)

  # Pattern source: SaveSceneDialog.tsx (Modal wrapper, backdrop, surface-container, header/footer)
  # Pattern source: BlockLibraryPanel.tsx (Modal with close header)
  # Colors: useColors() from @/hooks/use-colors
  # Styling: all inline objects (no className)
  # Pressable: remapProps(Pressable, { className: false }) is project-wide; use style={({pressed})=>({...})}
  </read_first>

  <action>
    Create `components/ui/ConfirmDialog.tsx` as a new file. Follow the exact Modal pattern from SaveSceneDialog.tsx:
    - Use `Modal` from `react-native` with `animationType="fade" transparent onRequestClose={onCancel}`
    - Use `useColors()` from `@/hooks/use-colors`
    - Backdrop style: `{ flex: 1, backgroundColor: colors.backdrop || 'rgba(0,0,0,0.7)', justifyContent: 'center', alignItems: 'center' }`
    - Inner container style: `{ width: '85%', maxWidth: 360, backgroundColor: colors['surface-container'] || colors.surface, borderRadius: 12, overflow: 'hidden' }`
    - Accept props: `ConfirmDialogProps` with `visible`, `title`, `message`, `confirmLabel?` (default 'Delete'), `cancelLabel?` (default 'Cancel'), `onConfirm`, `onCancel`, `destructive?` (default true)
    - Header section: title text at fontSize 17, fontWeight '700', color colors.foreground
    - Body section: message text at fontSize 14, color colors.muted, lineHeight 20
    - Footer section: flexDirection 'row', justifyContent 'flex-end', borderTop at colors.border. Cancel button with border; Confirm button with `backgroundColor: destructive ? (colors.error || '#ff6b6b') : colors.primary`
    - Export as named export: `export function ConfirmDialog(...)` — do NOT use default export
    - Style all Pressables with inline style objects. Destructive confirm button must use error-color background in destructive mode (default true). Cancel button must use borderWidth 1 borderColor colors.border.
    - Do NOT add `className` attributes.
    - Do NOT add empty state handling — the Modal component handles visibility via the `visible` prop.
  </action>

  <verify>
    <automated>grep -c "export function ConfirmDialog" components/ui/ConfirmDialog.tsx</automated>
    <human-check>npm run typecheck (if available) or npx tsc --noEmit --pretty components/ui/ConfirmDialog.tsx 2>&1 | head -20</human-check>
  </verify>

  <acceptance_criteria>
    - File `components/ui/ConfirmDialog.tsx` exists
    - Exports named `ConfirmDialog` function
    - Imports `Modal` from `react-native` and `useColors` from `@/hooks/use-colors`
    - Has `ConfirmDialogProps` interface with all 7 fields (visible, title, message, confirmLabel?, cancelLabel?, onConfirm, onCancel, destructive?)
    - Default values: confirmLabel='Delete', cancelLabel='Cancel', destructive=true
    - Modal pattern matches SaveSceneDialog: animationType="fade", transparent, onRequestClose=onCancel
    - Footer has two Pressables: Cancel (border) + Delete (error color background when destructive)
    - All styles are inline objects
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add isSaving flag to editor store</name>
  <files>stores/use-editor-store.ts</files>

  <read_first>
  # stores/use-editor-store.ts — Zustand store with create pattern
  # Interface EditorStore has scene state, view state, undo/redo stacks, and actions
  # All actions are defined in the interface then implemented in create()
  # Pattern for adding new state+action:
  #   1. Add field to EditorStore interface
  #   2. Add action method to EditorStore interface
  #   3. Add field to initialState
  #   4. Implement action in create() callback
  # 
  # Existing pattern:
  #   showMiniPreview: boolean;  // in interface
  #   setShowMiniPreview: (show: boolean) => void;  // in interface
  #   ... in initialState... showMiniPreview: true,
  #   ... in create... setShowMiniPreview: (show) => set({ showMiniPreview: show }),
  </read_first>

  <action>
    Add `isSaving` state and `setIsSaving` action to the editor store:
    1. In the `EditorStore` interface: add `isSaving: boolean;` and `setIsSaving: (saving: boolean) => void;`
    2. In `initialState`: add `isSaving: false,`
    3. In the `create()` callback: add `setIsSaving: (saving) => set({ isSaving: saving }),`

    Placement within EditorStore interface:
    - Add `isSaving` next to `blockSearchQuery` (after the view state fields, before undo/redo stacks)
    - Add `setIsSaving` next to `setBlockSearchQuery` (in the actions section, before undo/redo)

    Patterns to follow exactly:
    - Interface: `isSaving: boolean;` — same indentation level as neighboring fields
    - Interface: `setIsSaving: (saving: boolean) => void;`
    - initialState: `isSaving: false,` — placed alphabetically or logically near other view/status flags
    - Implementation: `setIsSaving: (saving) => set({ isSaving: saving }),`

    Do NOT:
    - Change any existing interface fields, actions, or behavior
    - Add logic beyond the simple state setter
    - Import any new dependencies
    - Use `saving` in a stale closure pattern (this is just a setter, no async involved)
  </action>

  <verify>
    <automated>grep -c "isSaving: boolean" stores/use-editor-store.ts; grep -c "isSaving: false" stores/use-editor-store.ts; grep -c "setIsSaving:" stores/use-editor-store.ts</automated>
  </verify>

  <acceptance_criteria>
    - `isSaving: boolean` appears in `EditorStore` interface
    - `setIsSaving: (saving: boolean) => void` appears in `EditorStore` interface
    - `isSaving: false` appears in `initialState`
    - `setIsSaving: (saving) => set({ isSaving: saving })` exists in the `create()` implementation
    - No other store behavior changed
    - grep counts return 3 (one for each pattern: interface state, interface action, initialState, implementation — counts vary but must show existence)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wrap SceneEditor with ErrorBoundary</name>
  <files>app/scene-editor.tsx</files>

  <read_first>
  # app/scene-editor.tsx:
  # - Default export SceneEditorScreen
  # - Imports SceneComposer from @/components/editor/SceneComposer
  # - Returns <SceneComposer storyId={storyId} sceneId={sceneId} initialSceneDraft={initialSceneDraft} />
  # 
  # components/ErrorBoundary.tsx:
  # - Class component with getDerivedStateFromError + componentDidCatch
  # - Accepts children (required), colors? (optional), fallback? (optional)
  # - Has resetError method for retry
  # - Default fallback shows "Щось пішло не так" with retry button
  # - Uses Ukrainian error messages
  # - Import: import { ErrorBoundary } from '@/components/ErrorBoundary'
  # - Usage: <ErrorBoundary children={<SceneComposer ... />} />
  </read_first>

  <action>
    Edit `app/scene-editor.tsx` to wrap `<SceneComposer>` with the existing `<ErrorBoundary>`:

    1. Add import: `import { ErrorBoundary } from '@/components/ErrorBoundary';` (at top, after the existing imports, in alphabetical order by source)
    2. Replace the direct `<SceneComposer>` usage with an ErrorBoundary wrapper:
       - Before: `<SceneComposer storyId={storyId} sceneId={sceneId} initialSceneDraft={initialSceneDraft} />`
       - After: `<ErrorBoundary><SceneComposer storyId={storyId} sceneId={sceneId} initialSceneDraft={initialSceneDraft} /></ErrorBoundary>`
    3. Do NOT pass a custom `fallback` prop — the ErrorBoundary's default Ukrainian fallback UI is sufficient. It shows "Щось пішло не так" (Something went wrong) + error details + retry button (`Спробувати знову`).
    4. Do NOT pass `colors` prop — the default ErrorBoundary uses hardcoded color fallbacks.
    5. Do NOT change any other part of scene-editor.tsx.
    6. Do NOT import ErrorBoundary types — the default import from `@/components/ErrorBoundary` is sufficient.
  </action>

  <verify>
    <automated>grep -c "ErrorBoundary" app/scene-editor.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `import { ErrorBoundary } from '@/components/ErrorBoundary'` added at top of file
    - `<SceneComposer ... />` wrapped with `<ErrorBoundary>...</ErrorBoundary>`
    - No custom `fallback` or `colors` prop passed to ErrorBoundary
    - No other code in scene-editor.tsx was modified
    - grep shows at least 2 matches (import + usage)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component boundary | ConfirmDialog is a presentational component — no external input crosses trust boundary |
| ErrorBoundary recovery | Reset action re-mounts children — no security implications as this is UI-only error handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
- All three tasks produce independent artifacts with no file conflicts
- ConfirmDialog: verify file exists with correct exports
- isSaving: verify store compiles with new field
- ErrorBoundary: verify scene-editor.tsx wraps SceneComposer
</verification>

<success_criteria>
- `components/ui/ConfirmDialog.tsx` created with Modal-based pattern
- `stores/use-editor-store.ts` has `isSaving` flag and `setIsSaving` action
- `app/scene-editor.tsx` wraps SceneComposer with ErrorBoundary
- All three artifacts ready for Plan 2 consumption
</success_criteria>

<output>
Create `.planning/phases/07-editor-ux-polish/07-01-SUMMARY.md` when done
</output>
